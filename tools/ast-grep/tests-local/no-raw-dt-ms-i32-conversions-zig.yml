id: no-raw-dt-ms-i32-conversions-zig
valid:
  - |
    fn ok(dt: f32) i32 {
        return ftolMsI32(dt);
    }
invalid:
  - |
    fn bad(dt: f32) i32 {
        return @intFromFloat(@round(dt * 1000.0));
    }
  - |
    fn bad2(dt: f32) i32 {
        return @intFromFloat(dt * 1000.0);
    }
