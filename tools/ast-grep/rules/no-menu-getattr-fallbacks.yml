id: no-menu-getattr-fallbacks
language: python
severity: warning
message: Menu rendering contracts are concrete; avoid `getattr` fallbacks in screen menu code.
files:
  - src/crimson/screens/menu.py
rule:
  pattern: getattr($OBJ, $ATTR, $DEFAULT)
