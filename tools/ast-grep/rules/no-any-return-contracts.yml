id: no-any-return-contracts
language: python
severity: warning
message: Avoid `-> Any` return contracts in runtime code; use precise types.
files:
  - src/**/*.py
rule:
  pattern: |
    def $FUNC($$$ARGS) -> Any:
      $$$BODY
