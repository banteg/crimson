id: no-dynamic-attr-hasattr-checks
snapshots:
  ? |
    if hasattr(parsed, "capture"):
      return parsed.capture
  : labels:
    - source: hasattr(parsed, "capture")
      style: primary
      start: 3
      end: 29
  ? |
    if hasattr(self._mode, "close_requested"):
      return True
  : labels:
    - source: hasattr(self._mode, "close_requested")
      style: primary
      start: 3
      end: 41
