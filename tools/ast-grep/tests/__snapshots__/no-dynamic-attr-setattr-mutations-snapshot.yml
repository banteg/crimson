id: no-dynamic-attr-setattr-mutations
snapshots:
  ? |
    setattr(world.state.rng, "rand", orig_rand)
  : labels:
    - source: setattr(world.state.rng, "rand", orig_rand)
      style: primary
      start: 0
      end: 43
  ? |
    setattr(world.state.rng, "rand", traced_rand)
  : labels:
    - source: setattr(world.state.rng, "rand", traced_rand)
      style: primary
      start: 0
      end: 45
