id: no-capture-typed-field-dict-assignment
valid:
  - |
    from tests.test_original_capture_conversion import _base_input_player_keys, _base_input_approx

    tick.input_player_keys = [_base_input_player_keys(player_index=0, fire_down=True)]
    tick.input_approx = [_base_input_approx(player_index=0, aim_x=512.0, aim_y=512.0)]
  - |
    from tests.builders.capture import build_capture_perk_apply_outside_before

    tick.perk_apply_outside_before = build_capture_perk_apply_outside_before(calls=1, dropped=0, head=[])
invalid:
  - |
    tick.input_player_keys = [{"player_index": 0, "fire_down": True}]
  - |
    tick.input_approx = [{"player_index": 0, "aim_x": 512.0, "aim_y": 512.0}]
  - |
    tick.perk_apply_outside_before = {"calls": 1, "dropped": 0, "head": []}
