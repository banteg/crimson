id: no-any-contract-gaps
valid:
  - |
    from typing import Protocol, TypeAlias

    class ConfigLike(Protocol):
      player_count: int

    GameplayState: TypeAlias = tuple[int, int]
  - |
    def bind_mode(mode: object) -> None:
      return
invalid:
  - |
    from typing import Any

    config: Any
  - |
    from typing import Any, TypeAlias

    GameplayState: TypeAlias = Any
  - |
    from typing import Any

    def bind_mode(mode: Any) -> None:
      return
