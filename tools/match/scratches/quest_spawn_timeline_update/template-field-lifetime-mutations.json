{
  "schema": 1,
  "sites": [
    {
      "name": "template-field-lifetime",
      "find": "        if (entry->count > 0) {\n            int *template_id = &entry->template_id;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    *template_id,\n                    (const vec2f_t *)&pos,\n                    entry->heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < entry->count);\n        }\n",
      "replacements": [
        {
          "name": "heading-template-record-pointer",
          "text": "        if (entry->count > 0) {\n            struct spawn_payload_t {\n                float heading;\n                int template_id;\n            };\n            spawn_payload_t *payload =\n                (spawn_payload_t *)&entry->heading;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    payload->template_id,\n                    (const vec2f_t *)&pos,\n                    payload->heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < entry->count);\n        }\n"
        },
        {
          "name": "heading-word-pointer",
          "text": "        if (entry->count > 0) {\n            int *payload = (int *)&entry->heading;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    payload[1],\n                    (const vec2f_t *)&pos,\n                    *(float *)&payload[0]);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < entry->count);\n        }\n"
        },
        {
          "name": "template-word-pointer",
          "text": "        if (entry->count > 0) {\n            int *payload = &entry->template_id;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    payload[0],\n                    (const vec2f_t *)&pos,\n                    *((float *)payload - 1));\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < entry->count);\n        }\n"
        },
        {
          "name": "template-byte-pointer",
          "text": "        if (entry->count > 0) {\n            unsigned char *payload =\n                (unsigned char *)&entry->template_id;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    *(int *)payload,\n                    (const vec2f_t *)&pos,\n                    *(float *)(payload - sizeof(float)));\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < entry->count);\n        }\n"
        },
        {
          "name": "split-heading-record-pointer",
          "text": "        if (entry->count > 0) {\n            struct spawn_payload_t {\n                float heading;\n                int template_id;\n            };\n            spawn_payload_t *payload;\n            int spread;\n\n            payload = (spawn_payload_t *)&entry->heading;\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    payload->template_id,\n                    (const vec2f_t *)&pos,\n                    payload->heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < entry->count);\n        }\n"
        },
        {
          "name": "heading-template-record-reference",
          "text": "        if (entry->count > 0) {\n            struct spawn_payload_t {\n                float heading;\n                int template_id;\n            };\n            spawn_payload_t &payload =\n                *(spawn_payload_t *)&entry->heading;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    payload.template_id,\n                    (const vec2f_t *)&pos,\n                    payload.heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < entry->count);\n        }\n"
        }
      ]
    }
  ]
}
