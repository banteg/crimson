{
  "schema": 1,
  "sites": [
    {
      "name": "spawn-parameter-owner-followup",
      "find": "#include \"crimsonland_gameplay.h\"\n\n#define CRIMSONLAND_USE_ORIGINAL_TERRAIN_OWNER\n#include \"crimsonland_terrain_owner.h\"\n\nstruct quest_timeline_vec2_t {\n    float x;\n    float y;\n\n    quest_timeline_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    quest_timeline_vec2_t operator+(const quest_timeline_vec2_t &other) const\n    {\n        return quest_timeline_vec2_t(x + other.x, y + other.y);\n    }\n};\n\nextern \"C\" int frame_dt_ms;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int quest_spawn_stall_timer_ms;\n\nextern \"C\" void quest_spawn_timeline_update(void)\n{\n    unsigned char creatures_none_active = creatures_any_active_flag;\n    if (creatures_none_active) {\n        quest_spawn_stall_timer_ms += frame_dt_ms;\n    } else {\n        quest_spawn_stall_timer_ms = 0;\n    }\n\n    int entry_count = quest_spawn_count;\n    int entry_index = 0;\n    if (entry_count <= 0) {\n        return;\n    }\n\n    int timeline = quest_spawn_timeline;\n    int *trigger_cursor = &quest_spawn_table[0].trigger_time_ms;\n    while (entry_index < entry_count) {\n        if (trigger_cursor[1] > 0) {\n            if (trigger_cursor[0] < timeline) {\n                goto spawn_entries;\n            }\n            if (creatures_none_active\n                && quest_spawn_stall_timer_ms > 3000\n                && timeline > 0x6a4) {\n                goto spawn_entries;\n            }\n        }\n        ++entry_index;\n        trigger_cursor += sizeof(quest_spawn_entry_t) / sizeof(int);\n    }\n    return;\n\nspawn_entries:\n    quest_timeline_vec2_t zero_offset(0.0f, 0.0f);\n    quest_spawn_entry_t *entry = &quest_spawn_table[entry_index];\n    do {\n        quest_timeline_vec2_t offset = zero_offset;\n        int spawn_index = 0;\n        if (entry->count > 0) {\n            int *template_id = &entry->template_id;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    *template_id,\n                    (const vec2f_t *)&pos,\n                    entry->heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < entry->count);\n        }\n\n        entry_count = quest_spawn_count;\n        entry->count = 0;\n        creatures_any_active_flag = 0;\n        if (entry_index >= entry_count - 1) {\n            return;\n        }\n        if (entry->trigger_time_ms != entry[1].trigger_time_ms) {\n            return;\n        }\n        ++entry_index;\n        ++entry;\n    } while (true);\n}\n",
      "replacements": [
        {
          "name": "heading_first_inner",
          "text": "#include \"crimsonland_gameplay.h\"\n\n#define CRIMSONLAND_USE_ORIGINAL_TERRAIN_OWNER\n#include \"crimsonland_terrain_owner.h\"\n\nstruct quest_timeline_vec2_t {\n    float x;\n    float y;\n\n    quest_timeline_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    quest_timeline_vec2_t operator+(const quest_timeline_vec2_t &other) const\n    {\n        return quest_timeline_vec2_t(x + other.x, y + other.y);\n    }\n};\n\nstruct quest_spawn_params { float heading; int template_id; int time; int count; };\n\nextern \"C\" int frame_dt_ms;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int quest_spawn_stall_timer_ms;\n\nextern \"C\" void quest_spawn_timeline_update(void)\n{\n    unsigned char creatures_none_active = creatures_any_active_flag;\n    if (creatures_none_active) {\n        quest_spawn_stall_timer_ms += frame_dt_ms;\n    } else {\n        quest_spawn_stall_timer_ms = 0;\n    }\n\n    int entry_count = quest_spawn_count;\n    int entry_index = 0;\n    if (entry_count <= 0) {\n        return;\n    }\n\n    int timeline = quest_spawn_timeline;\n    int *trigger_cursor = &quest_spawn_table[0].trigger_time_ms;\n    while (entry_index < entry_count) {\n        if (trigger_cursor[1] > 0) {\n            if (trigger_cursor[0] < timeline) {\n                goto spawn_entries;\n            }\n            if (creatures_none_active\n                && quest_spawn_stall_timer_ms > 3000\n                && timeline > 0x6a4) {\n                goto spawn_entries;\n            }\n        }\n        ++entry_index;\n        trigger_cursor += sizeof(quest_spawn_entry_t) / sizeof(int);\n    }\n    return;\n\nspawn_entries:\n    quest_timeline_vec2_t zero_offset(0.0f, 0.0f);\n    quest_spawn_entry_t *entry = &quest_spawn_table[entry_index];\n    do {\n        quest_timeline_vec2_t offset = zero_offset;\n        int spawn_index = 0;\n        if (entry->count > 0) {\n            quest_spawn_params *params = (quest_spawn_params *)&entry->heading;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    params->template_id,\n                    (const vec2f_t *)&pos,\n                    params->heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < params->count);\n        }\n\n        entry_count = quest_spawn_count;\n        entry->count = 0;\n        creatures_any_active_flag = 0;\n        if (entry_index >= entry_count - 1) {\n            return;\n        }\n        if (entry->trigger_time_ms != entry[1].trigger_time_ms) {\n            return;\n        }\n        ++entry_index;\n        ++entry;\n    } while (true);\n}\n"
        },
        {
          "name": "heading_first_outer",
          "text": "#include \"crimsonland_gameplay.h\"\n\n#define CRIMSONLAND_USE_ORIGINAL_TERRAIN_OWNER\n#include \"crimsonland_terrain_owner.h\"\n\nstruct quest_timeline_vec2_t {\n    float x;\n    float y;\n\n    quest_timeline_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    quest_timeline_vec2_t operator+(const quest_timeline_vec2_t &other) const\n    {\n        return quest_timeline_vec2_t(x + other.x, y + other.y);\n    }\n};\n\nstruct quest_spawn_params { float heading; int template_id; int time; int count; };\n\nextern \"C\" int frame_dt_ms;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int quest_spawn_stall_timer_ms;\n\nextern \"C\" void quest_spawn_timeline_update(void)\n{\n    unsigned char creatures_none_active = creatures_any_active_flag;\n    if (creatures_none_active) {\n        quest_spawn_stall_timer_ms += frame_dt_ms;\n    } else {\n        quest_spawn_stall_timer_ms = 0;\n    }\n\n    int entry_count = quest_spawn_count;\n    int entry_index = 0;\n    if (entry_count <= 0) {\n        return;\n    }\n\n    int timeline = quest_spawn_timeline;\n    int *trigger_cursor = &quest_spawn_table[0].trigger_time_ms;\n    while (entry_index < entry_count) {\n        if (trigger_cursor[1] > 0) {\n            if (trigger_cursor[0] < timeline) {\n                goto spawn_entries;\n            }\n            if (creatures_none_active\n                && quest_spawn_stall_timer_ms > 3000\n                && timeline > 0x6a4) {\n                goto spawn_entries;\n            }\n        }\n        ++entry_index;\n        trigger_cursor += sizeof(quest_spawn_entry_t) / sizeof(int);\n    }\n    return;\n\nspawn_entries:\n    quest_timeline_vec2_t zero_offset(0.0f, 0.0f);\n    quest_spawn_entry_t *entry = &quest_spawn_table[entry_index];\n    do {\n        quest_timeline_vec2_t offset = zero_offset;\n        int spawn_index = 0;\n        quest_spawn_params *params = (quest_spawn_params *)&entry->heading;\n        if (params->count > 0) {\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    params->template_id,\n                    (const vec2f_t *)&pos,\n                    params->heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < params->count);\n        }\n\n        entry_count = quest_spawn_count;\n        entry->count = 0;\n        creatures_any_active_flag = 0;\n        if (entry_index >= entry_count - 1) {\n            return;\n        }\n        if (entry->trigger_time_ms != entry[1].trigger_time_ms) {\n            return;\n        }\n        ++entry_index;\n        ++entry;\n    } while (true);\n}\n"
        },
        {
          "name": "id_first_inner",
          "text": "#include \"crimsonland_gameplay.h\"\n\n#define CRIMSONLAND_USE_ORIGINAL_TERRAIN_OWNER\n#include \"crimsonland_terrain_owner.h\"\n\nstruct quest_timeline_vec2_t {\n    float x;\n    float y;\n\n    quest_timeline_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    quest_timeline_vec2_t operator+(const quest_timeline_vec2_t &other) const\n    {\n        return quest_timeline_vec2_t(x + other.x, y + other.y);\n    }\n};\n\nstruct quest_spawn_params { int template_id; int time; int count; };\n\nextern \"C\" int frame_dt_ms;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int quest_spawn_stall_timer_ms;\n\nextern \"C\" void quest_spawn_timeline_update(void)\n{\n    unsigned char creatures_none_active = creatures_any_active_flag;\n    if (creatures_none_active) {\n        quest_spawn_stall_timer_ms += frame_dt_ms;\n    } else {\n        quest_spawn_stall_timer_ms = 0;\n    }\n\n    int entry_count = quest_spawn_count;\n    int entry_index = 0;\n    if (entry_count <= 0) {\n        return;\n    }\n\n    int timeline = quest_spawn_timeline;\n    int *trigger_cursor = &quest_spawn_table[0].trigger_time_ms;\n    while (entry_index < entry_count) {\n        if (trigger_cursor[1] > 0) {\n            if (trigger_cursor[0] < timeline) {\n                goto spawn_entries;\n            }\n            if (creatures_none_active\n                && quest_spawn_stall_timer_ms > 3000\n                && timeline > 0x6a4) {\n                goto spawn_entries;\n            }\n        }\n        ++entry_index;\n        trigger_cursor += sizeof(quest_spawn_entry_t) / sizeof(int);\n    }\n    return;\n\nspawn_entries:\n    quest_timeline_vec2_t zero_offset(0.0f, 0.0f);\n    quest_spawn_entry_t *entry = &quest_spawn_table[entry_index];\n    do {\n        quest_timeline_vec2_t offset = zero_offset;\n        int spawn_index = 0;\n        if (entry->count > 0) {\n            quest_spawn_params *params = (quest_spawn_params *)&entry->template_id;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    params->template_id,\n                    (const vec2f_t *)&pos,\n                    entry->heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < params->count);\n        }\n\n        entry_count = quest_spawn_count;\n        entry->count = 0;\n        creatures_any_active_flag = 0;\n        if (entry_index >= entry_count - 1) {\n            return;\n        }\n        if (entry->trigger_time_ms != entry[1].trigger_time_ms) {\n            return;\n        }\n        ++entry_index;\n        ++entry;\n    } while (true);\n}\n"
        },
        {
          "name": "id_first_outer",
          "text": "#include \"crimsonland_gameplay.h\"\n\n#define CRIMSONLAND_USE_ORIGINAL_TERRAIN_OWNER\n#include \"crimsonland_terrain_owner.h\"\n\nstruct quest_timeline_vec2_t {\n    float x;\n    float y;\n\n    quest_timeline_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    quest_timeline_vec2_t operator+(const quest_timeline_vec2_t &other) const\n    {\n        return quest_timeline_vec2_t(x + other.x, y + other.y);\n    }\n};\n\nstruct quest_spawn_params { int template_id; int time; int count; };\n\nextern \"C\" int frame_dt_ms;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int quest_spawn_stall_timer_ms;\n\nextern \"C\" void quest_spawn_timeline_update(void)\n{\n    unsigned char creatures_none_active = creatures_any_active_flag;\n    if (creatures_none_active) {\n        quest_spawn_stall_timer_ms += frame_dt_ms;\n    } else {\n        quest_spawn_stall_timer_ms = 0;\n    }\n\n    int entry_count = quest_spawn_count;\n    int entry_index = 0;\n    if (entry_count <= 0) {\n        return;\n    }\n\n    int timeline = quest_spawn_timeline;\n    int *trigger_cursor = &quest_spawn_table[0].trigger_time_ms;\n    while (entry_index < entry_count) {\n        if (trigger_cursor[1] > 0) {\n            if (trigger_cursor[0] < timeline) {\n                goto spawn_entries;\n            }\n            if (creatures_none_active\n                && quest_spawn_stall_timer_ms > 3000\n                && timeline > 0x6a4) {\n                goto spawn_entries;\n            }\n        }\n        ++entry_index;\n        trigger_cursor += sizeof(quest_spawn_entry_t) / sizeof(int);\n    }\n    return;\n\nspawn_entries:\n    quest_timeline_vec2_t zero_offset(0.0f, 0.0f);\n    quest_spawn_entry_t *entry = &quest_spawn_table[entry_index];\n    do {\n        quest_timeline_vec2_t offset = zero_offset;\n        int spawn_index = 0;\n        quest_spawn_params *params = (quest_spawn_params *)&entry->template_id;\n        if (params->count > 0) {\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    params->template_id,\n                    (const vec2f_t *)&pos,\n                    entry->heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < params->count);\n        }\n\n        entry_count = quest_spawn_count;\n        entry->count = 0;\n        creatures_any_active_flag = 0;\n        if (entry_index >= entry_count - 1) {\n            return;\n        }\n        if (entry->trigger_time_ms != entry[1].trigger_time_ms) {\n            return;\n        }\n        ++entry_index;\n        ++entry;\n    } while (true);\n}\n"
        },
        {
          "name": "count_4",
          "text": "#include \"crimsonland_gameplay.h\"\n\n#define CRIMSONLAND_USE_ORIGINAL_TERRAIN_OWNER\n#include \"crimsonland_terrain_owner.h\"\n\nstruct quest_timeline_vec2_t {\n    float x;\n    float y;\n\n    quest_timeline_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    quest_timeline_vec2_t operator+(const quest_timeline_vec2_t &other) const\n    {\n        return quest_timeline_vec2_t(x + other.x, y + other.y);\n    }\n};\n\nextern \"C\" int frame_dt_ms;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int quest_spawn_stall_timer_ms;\n\nextern \"C\" void quest_spawn_timeline_update(void)\n{\n    unsigned char creatures_none_active = creatures_any_active_flag;\n    if (creatures_none_active) {\n        quest_spawn_stall_timer_ms += frame_dt_ms;\n    } else {\n        quest_spawn_stall_timer_ms = 0;\n    }\n\n    int entry_count = quest_spawn_count;\n    int entry_index = 0;\n    if (entry_count <= 0) {\n        return;\n    }\n\n    int timeline = quest_spawn_timeline;\n    int *trigger_cursor = &quest_spawn_table[0].trigger_time_ms;\n    while (entry_index < entry_count) {\n        if (trigger_cursor[1] > 0) {\n            if (trigger_cursor[0] < timeline) {\n                goto spawn_entries;\n            }\n            if (creatures_none_active\n                && quest_spawn_stall_timer_ms > 3000\n                && timeline > 0x6a4) {\n                goto spawn_entries;\n            }\n        }\n        ++entry_index;\n        trigger_cursor += sizeof(quest_spawn_entry_t) / sizeof(int);\n    }\n    return;\n\nspawn_entries:\n    quest_timeline_vec2_t zero_offset(0.0f, 0.0f);\n    quest_spawn_entry_t *entry = &quest_spawn_table[entry_index];\n    do {\n        quest_timeline_vec2_t offset = zero_offset;\n        int spawn_index = 0;\n        if (entry->count > 0) {\n            int *template_id = &entry->template_id;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    *template_id,\n                    (const vec2f_t *)&pos,\n                    entry->heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < template_id[2]);\n        }\n\n        entry_count = quest_spawn_count;\n        entry->count = 0;\n        creatures_any_active_flag = 0;\n        if (entry_index >= entry_count - 1) {\n            return;\n        }\n        if (entry->trigger_time_ms != entry[1].trigger_time_ms) {\n            return;\n        }\n        ++entry_index;\n        ++entry;\n    } while (true);\n}\n"
        },
        {
          "name": "count_5",
          "text": "#include \"crimsonland_gameplay.h\"\n\n#define CRIMSONLAND_USE_ORIGINAL_TERRAIN_OWNER\n#include \"crimsonland_terrain_owner.h\"\n\nstruct quest_timeline_vec2_t {\n    float x;\n    float y;\n\n    quest_timeline_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    quest_timeline_vec2_t operator+(const quest_timeline_vec2_t &other) const\n    {\n        return quest_timeline_vec2_t(x + other.x, y + other.y);\n    }\n};\n\nextern \"C\" int frame_dt_ms;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int quest_spawn_stall_timer_ms;\n\nextern \"C\" void quest_spawn_timeline_update(void)\n{\n    unsigned char creatures_none_active = creatures_any_active_flag;\n    if (creatures_none_active) {\n        quest_spawn_stall_timer_ms += frame_dt_ms;\n    } else {\n        quest_spawn_stall_timer_ms = 0;\n    }\n\n    int entry_count = quest_spawn_count;\n    int entry_index = 0;\n    if (entry_count <= 0) {\n        return;\n    }\n\n    int timeline = quest_spawn_timeline;\n    int *trigger_cursor = &quest_spawn_table[0].trigger_time_ms;\n    while (entry_index < entry_count) {\n        if (trigger_cursor[1] > 0) {\n            if (trigger_cursor[0] < timeline) {\n                goto spawn_entries;\n            }\n            if (creatures_none_active\n                && quest_spawn_stall_timer_ms > 3000\n                && timeline > 0x6a4) {\n                goto spawn_entries;\n            }\n        }\n        ++entry_index;\n        trigger_cursor += sizeof(quest_spawn_entry_t) / sizeof(int);\n    }\n    return;\n\nspawn_entries:\n    quest_timeline_vec2_t zero_offset(0.0f, 0.0f);\n    quest_spawn_entry_t *entry = &quest_spawn_table[entry_index];\n    do {\n        quest_timeline_vec2_t offset = zero_offset;\n        int spawn_index = 0;\n        if (entry->count > 0) {\n            int *template_id = &entry->template_id;\n            int spread;\n\n            spread = 0;\n            do {\n                if (entry->position.x < 0.0f\n                    || (float)terrain_texture_width < entry->position.x) {\n                    offset.y = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.y = -offset.y;\n                    }\n                } else {\n                    offset.x = (float)spread;\n                    if (spawn_index & 1) {\n                        offset.x = -offset.x;\n                    }\n                }\n\n                quest_timeline_vec2_t pos(\n                    offset.x + entry->position.x,\n                    offset.y + entry->position.y);\n                creature_spawn_template(\n                    *template_id,\n                    (const vec2f_t *)&pos,\n                    entry->heading);\n\n                ++spawn_index;\n                spread += 0x28;\n            } while (spawn_index < ((quest_spawn_entry_t *)((char *)template_id - 12))->count);\n        }\n\n        entry_count = quest_spawn_count;\n        entry->count = 0;\n        creatures_any_active_flag = 0;\n        if (entry_index >= entry_count - 1) {\n            return;\n        }\n        if (entry->trigger_time_ms != entry[1].trigger_time_ms) {\n            return;\n        }\n        ++entry_index;\n        ++entry;\n    } while (true);\n}\n"
        }
      ]
    }
  ]
}
