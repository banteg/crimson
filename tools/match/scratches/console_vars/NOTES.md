# console_vars

Native target: `crimsonland.exe` at `0x004013c0` (65 bytes).

The command walks the cvar list, prints every name, and prints the final count.
Natural C++ matches all 24 instructions and references `7/0/0`.
