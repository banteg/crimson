# `grim_pixel_format_ctor_dxt4`

Native target: `grim.dll` at `0x1001b442` (24 bytes).

This derived constructor delegates the descriptor to the shared DXT-format
initializer and installs the DXT4 vtable.
