# console_cvar_unregister

Native target: `crimsonland.exe` at `0x004024e0` (72 bytes).

This is the cvar-list twin of command unregister: resolve by name, update the
head directly, or splice out a later node through its predecessor. Natural
C++ matches all 32 instructions and references `1/0/0`.
