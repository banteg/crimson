# audio_init_music

Skips initialization when either audio domain is disabled, resets the mute and
volume tables, selects the packed or loose music resource path, loads the fixed
intro/theme tracks, executes the tune list, derives the two extra-track ids from
the quest track, and finally marks SFX playback audible.
