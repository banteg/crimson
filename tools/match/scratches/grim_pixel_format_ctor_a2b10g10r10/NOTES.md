# `grim_pixel_format_ctor_a2b10g10r10`

Native target: `grim.dll` at `0x1001aba3` (28 bytes).

This derived pixel-format constructor delegates `(desc, 32, 1)` to the common
format base and installs the A2B10G10R10 vtable.
