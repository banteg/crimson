# `grim_pixel_format_ctor_r5g6b5`

Native target: `grim.dll` at `0x1001a579` (28 bytes).

This derived pixel-format constructor delegates `(desc, 16, 1)` to the common
format base and installs the R5G6B5 vtable.
