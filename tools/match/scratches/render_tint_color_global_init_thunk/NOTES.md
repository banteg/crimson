# `render_tint_color_global_init_thunk`

Native target: `crimsonland.exe` at `0x0041e070` (5 bytes).

The CRT initializer table points at this tail-call thunk, which forwards to
`render_tint_color_global_init`.
