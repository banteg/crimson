# console_cvar_autocomplete

Native target: `crimsonland.exe` at `0x00402630` (164 bytes).

An empty query returns null. The method first searches the cvar list for an
exact name and only then restarts at the head for a prefix match of the query
length. Natural C++ matches all 83 instructions and references `1/0/0`.
