{
  "schema": 1,
  "sites": [
    {
      "name": "vector-copy-constructor",
      "find": "    projectile_render_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    projectile_render_vec2_t operator+(",
      "replacements": [
        {
          "name": "direct-members",
          "text": "    projectile_render_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    projectile_render_vec2_t(\n        const projectile_render_vec2_t &other)\n        : x(other.x), y(other.y) {}\n\n    projectile_render_vec2_t operator+("
        },
        {
          "name": "y-first-members",
          "text": "    projectile_render_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    projectile_render_vec2_t(\n        const projectile_render_vec2_t &other)\n        : y(other.y), x(other.x) {}\n\n    projectile_render_vec2_t operator+("
        },
        {
          "name": "assigned-members",
          "text": "    projectile_render_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    projectile_render_vec2_t(\n        const projectile_render_vec2_t &other)\n    {\n        x = other.x;\n        y = other.y;\n    }\n\n    projectile_render_vec2_t operator+("
        },
        {
          "name": "y-first-assigned-members",
          "text": "    projectile_render_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}\n\n    projectile_render_vec2_t(\n        const projectile_render_vec2_t &other)\n    {\n        y = other.y;\n        x = other.x;\n    }\n\n    projectile_render_vec2_t operator+("
        }
      ]
    }
  ]
}
