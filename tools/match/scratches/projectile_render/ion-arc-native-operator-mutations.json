{
  "schema": 1,
  "sites": [
    {
      "name": "ion-arc-native-perpendicular-and-points",
      "find": "                    projectile_render_vec2_t side(-arc.y, arc.x);\n                    side = side * effect_scale;\n                    projectile_render_vec2_t start =\n                        camera_offset\n                        + *(projectile_render_vec2_t *)&projectile->position;\n                    projectile_render_vec2_t end =\n                        camera_offset\n                        + *(projectile_render_vec2_t *)\n                             &creature_pool[creature_index].pos_x;\n                    projectile_render_vec2_t strip0 =\n                        start - side * 10.0f;\n                    projectile_render_vec2_t strip1 =\n                        start + side * 10.0f;\n                    projectile_render_vec2_t strip2 =\n                        end + side * 10.0f;\n                    projectile_render_vec2_t strip3 =\n                        end - side * 10.0f;",
      "replacements": [
        {
          "name": "inplace-direct-chained",
          "text": "                    float old_arc_x = arc.x;\n                    arc.x = -arc.y;\n                    arc.y = old_arc_x;\n                    projectile_render_vec2_t start =\n                        camera_offset\n                        + *(projectile_render_vec2_t *)&projectile->position;\n                    projectile_render_vec2_t strip0 =\n                        start - arc * effect_scale * 10.0f;\n                    projectile_render_vec2_t strip1 =\n                        start + arc * effect_scale * 10.0f;\n                    projectile_render_vec2_t end =\n                        camera_offset\n                        + *(projectile_render_vec2_t *)\n                             &creature_pool[creature_index].pos_x;\n                    projectile_render_vec2_t strip2 =\n                        end + arc * effect_scale * 10.0f;\n                    projectile_render_vec2_t strip3 =\n                        end - arc * effect_scale * 10.0f;"
        },
        {
          "name": "inplace-direct-scaled-side",
          "text": "                    float old_arc_x = arc.x;\n                    arc.x = -arc.y;\n                    arc.y = old_arc_x;\n                    projectile_render_vec2_t start =\n                        camera_offset\n                        + *(projectile_render_vec2_t *)&projectile->position;\n                    projectile_render_vec2_t side = arc * effect_scale;\n                    projectile_render_vec2_t strip0 =\n                        start - side * 10.0f;\n                    projectile_render_vec2_t strip1 =\n                        start + side * 10.0f;\n                    projectile_render_vec2_t end =\n                        camera_offset\n                        + *(projectile_render_vec2_t *)\n                             &creature_pool[creature_index].pos_x;\n                    projectile_render_vec2_t strip2 =\n                        end + side * 10.0f;\n                    projectile_render_vec2_t strip3 =\n                        end - side * 10.0f;"
        },
        {
          "name": "inplace-copy-mutate-chained",
          "text": "                    float old_arc_x = arc.x;\n                    arc.x = -arc.y;\n                    arc.y = old_arc_x;\n                    projectile_render_vec2_t start =\n                        camera_offset\n                        + *(projectile_render_vec2_t *)&projectile->position;\n                    projectile_render_vec2_t strip0 = start;\n                    projectile_render_vec2_t strip1 = start;\n                    strip0 -= arc * effect_scale * 10.0f;\n                    strip1 += arc * effect_scale * 10.0f;\n                    projectile_render_vec2_t end =\n                        camera_offset\n                        + *(projectile_render_vec2_t *)\n                             &creature_pool[creature_index].pos_x;\n                    projectile_render_vec2_t strip2 = end;\n                    projectile_render_vec2_t strip3 = end;\n                    strip2 += arc * effect_scale * 10.0f;\n                    strip3 -= arc * effect_scale * 10.0f;"
        },
        {
          "name": "inplace-named-width-after-start",
          "text": "                    float old_arc_x = arc.x;\n                    arc.x = -arc.y;\n                    arc.y = old_arc_x;\n                    projectile_render_vec2_t start =\n                        camera_offset\n                        + *(projectile_render_vec2_t *)&projectile->position;\n                    projectile_render_vec2_t width =\n                        arc * effect_scale * 10.0f;\n                    projectile_render_vec2_t strip0 = start - width;\n                    projectile_render_vec2_t strip1 = start + width;\n                    projectile_render_vec2_t end =\n                        camera_offset\n                        + *(projectile_render_vec2_t *)\n                             &creature_pool[creature_index].pos_x;\n                    projectile_render_vec2_t strip2 = end + width;\n                    projectile_render_vec2_t strip3 = end - width;"
        }
      ]
    },
    {
      "name": "ion-arc-native-widen",
      "find": "                    projectile_render_vec2_t widen = side * 4.0f;",
      "replacements": [
        {
          "name": "arc-scale-chain",
          "text": "                    projectile_render_vec2_t widen =\n                        arc * effect_scale * 4.0f;"
        },
        {
          "name": "arc-scale-named-side",
          "text": "                    projectile_render_vec2_t widen_side =\n                        arc * effect_scale;\n                    projectile_render_vec2_t widen = widen_side * 4.0f;"
        }
      ]
    }
  ]
}
