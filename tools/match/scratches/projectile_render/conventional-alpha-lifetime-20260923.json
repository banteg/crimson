{
  "schema": 1,
  "sites": [
    {
      "name": "conventional-alpha-lifetime",
      "find": "        float alpha = projectile_render_clamp(\n            tail->life_timer);\n        grim_interface_ptr->grim_set_color_slot(\n",
      "replacements": [
        {
          "name": "direct-two-if",
          "text": "        float alpha = tail->life_timer;\n        if (alpha > 1.0f) alpha = 1.0f;\n        if (alpha < 0.0f) alpha = 0.0f;\n        grim_interface_ptr->grim_set_color_slot(\n"
        },
        {
          "name": "direct-if-else-if",
          "text": "        float alpha = tail->life_timer;\n        if (alpha > 1.0f) alpha = 1.0f;\n        else if (alpha < 0.0f) alpha = 0.0f;\n        grim_interface_ptr->grim_set_color_slot(\n"
        },
        {
          "name": "named-life-then-helper",
          "text": "        float live_timer = tail->life_timer;\n        float alpha = projectile_render_clamp(live_timer);\n        grim_interface_ptr->grim_set_color_slot(\n"
        },
        {
          "name": "ternary-two-stage",
          "text": "        float alpha = tail->life_timer;\n        alpha = alpha > 1.0f ? 1.0f : alpha;\n        alpha = alpha < 0.0f ? 0.0f : alpha;\n        grim_interface_ptr->grim_set_color_slot(\n"
        }
      ]
    }
  ]
}