{
  "schema": 1,
  "sites": [
    {
      "name": "batch-01-focused-value-boundaries",
      "find": "    buffer = &entry->buffers[0];\n    if (dsound_restore_buffer(*buffer)) {\n        if (entry->vorbis_stream != 0) {\n            goto stream_playback;\n        }\n        sfx_entry_upload_buffer(entry);\n    }\n\n    result = 0;\n    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    }\n\n    while (result < 16) {\n        if (entry->buffers[result] != 0) {\n            entry->buffers[result]->GetStatus(&status);\n            if ((status & DSBSTATUS_PLAYING) == 0) {\n                goto play_voice;\n            }\n        }\n        ++result;\n    }\n    result = rand() % 16;\n    entry->buffers[result]->Stop();\n\nplay_voice:\n    entry->buffers[result]->SetFrequency(sfx_rate_scale);\n    entry->buffers[result]->Play(0, 0, 0);\n    return result;\n",
      "replacements": [
        {
          "name": "initialized-common-result-tail",
          "text": "    buffer = &entry->buffers[0];\n    if (dsound_restore_buffer(*buffer)) {\n        if (entry->vorbis_stream != 0) {\n            goto stream_playback;\n        }\n        sfx_entry_upload_buffer(entry);\n    }\n\n    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        result = 0;\n        goto finished;\n    }\n\n    result = 0;\n    while (result < 16) {\n        if (entry->buffers[result] != 0) {\n            entry->buffers[result]->GetStatus(&status);\n            if ((status & DSBSTATUS_PLAYING) == 0) {\n                goto play_voice;\n            }\n        }\n        ++result;\n    }\n    result = rand() % 16;\n    entry->buffers[result]->Stop();\n\nplay_voice:\n    entry->buffers[result]->SetFrequency(sfx_rate_scale);\n    entry->buffers[result]->Play(0, 0, 0);\nfinished:\n    return result;\n"
        },
        {
          "name": "resident-counted-for",
          "text": "    buffer = &entry->buffers[0];\n    if (dsound_restore_buffer(*buffer)) {\n        if (entry->vorbis_stream != 0) {\n            goto stream_playback;\n        }\n        sfx_entry_upload_buffer(entry);\n    }\n\n    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    }\n\n    for (result = 0; result < 16; ++result) {\n        if (entry->buffers[result] != 0) {\n            entry->buffers[result]->GetStatus(&status);\n            if ((status & DSBSTATUS_PLAYING) == 0) {\n                goto play_voice;\n            }\n        }\n    }\n    result = rand() % 16;\n    entry->buffers[result]->Stop();\n\nplay_voice:\n    entry->buffers[result]->SetFrequency(sfx_rate_scale);\n    entry->buffers[result]->Play(0, 0, 0);\n    return result;\n"
        },
        {
          "name": "stream-else-resident",
          "text": "    buffer = &entry->buffers[0];\n    if (dsound_restore_buffer(*buffer)) {\n        if (entry->vorbis_stream != 0) {\n            goto stream_playback;\n        }\n        sfx_entry_upload_buffer(entry);\n    }\n\n    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    } else {\n\n    result = 0;\n    while (result < 16) {\n        if (entry->buffers[result] != 0) {\n            entry->buffers[result]->GetStatus(&status);\n            if ((status & DSBSTATUS_PLAYING) == 0) {\n                goto play_voice;\n            }\n        }\n        ++result;\n    }\n    result = rand() % 16;\n    entry->buffers[result]->Stop();\n\nplay_voice:\n    entry->buffers[result]->SetFrequency(sfx_rate_scale);\n    entry->buffers[result]->Play(0, 0, 0);\n    return result;\n    }\n"
        },
        {
          "name": "restore-reload-explicit-stream-value",
          "text": "    buffer = entry->buffers;\n    if (dsound_restore_buffer(*buffer)) {\n        if (entry->vorbis_stream != 0) {\n            goto stream_playback;\n        }\n        sfx_entry_upload_buffer(entry);\n    }\n\n    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    }\n\n    result = 0;\n    while (result < 16) {\n        if (entry->buffers[result] != 0) {\n            entry->buffers[result]->GetStatus(&status);\n            if ((status & DSBSTATUS_PLAYING) == 0) {\n                goto play_voice;\n            }\n        }\n        ++result;\n    }\n    result = rand() % 16;\n    entry->buffers[result]->Stop();\n\nplay_voice:\n    entry->buffers[result]->SetFrequency(sfx_rate_scale);\n    entry->buffers[result]->Play(0, 0, 0);\n    return result;\n"
        },
        {
          "name": "stream-resident-shared-initialized-return",
          "text": "    buffer = &entry->buffers[0];\n    if (dsound_restore_buffer(*buffer)) {\n        if (entry->vorbis_stream != 0) {\n            goto stream_playback;\n        }\n        sfx_entry_upload_buffer(entry);\n    }\n\n    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    }\n\n    result = 0;\n    while (result < 16) {\n        if (entry->buffers[result] != 0) {\n            entry->buffers[result]->GetStatus(&status);\n            if ((status & DSBSTATUS_PLAYING) == 0) {\n                goto play_voice;\n            }\n        }\n        ++result;\n    }\n    result = rand() % 16;\n    entry->buffers[result]->Stop();\n\nplay_voice:\n    entry->buffers[result]->SetFrequency(sfx_rate_scale);\n    entry->buffers[result]->Play(0, 0, 0);\n    return result;\n"
        }
      ]
    }
  ]
}
