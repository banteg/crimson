{
  "schema": 1,
  "sites": [
    {
      "name": "resident-index-lifetime",
      "find": "    result = 0;\n    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    }\n\n",
      "replacements": [
        {
          "name": "initialize-after-stream-return",
          "text": "    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    }\n\n    result = 0;\n"
        }
      ]
    },
    {
      "name": "resident-return-ownership",
      "find": "    while (result < 16) {\n        if (entry->buffers[result] != 0) {\n            entry->buffers[result]->GetStatus(&status);\n            if ((status & DSBSTATUS_PLAYING) == 0) {\n                goto play_voice;\n            }\n        }\n        ++result;\n    }\n    result = rand() % 16;\n    entry->buffers[result]->Stop();\n\nplay_voice:\n    entry->buffers[result]->SetFrequency(sfx_rate_scale);\n    entry->buffers[result]->Play(0, 0, 0);\n    return result;\n}\n",
      "replacements": [
        {
          "name": "play-and-return-at-selection",
          "text": "    while (result < 16) {\n        if (entry->buffers[result] != 0) {\n            entry->buffers[result]->GetStatus(&status);\n            if ((status & DSBSTATUS_PLAYING) == 0) {\n                entry->buffers[result]->SetFrequency(sfx_rate_scale);\n                entry->buffers[result]->Play(0, 0, 0);\n                return result;\n            }\n        }\n        ++result;\n    }\n    result = rand() % 16;\n    entry->buffers[result]->Stop();\n\n    entry->buffers[result]->SetFrequency(sfx_rate_scale);\n    entry->buffers[result]->Play(0, 0, 0);\n    return result;\n}\n"
        }
      ]
    }
  ]
}
