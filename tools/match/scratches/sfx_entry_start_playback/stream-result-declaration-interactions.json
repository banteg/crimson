{
  "schema": 1,
  "sites": [
    {
      "name": "local-declaration-order",
      "find": "    LPDIRECTSOUNDBUFFER *buffer;\n    DWORD status;\n    int result;",
      "replacements": [
        {
          "name": "buffer-result-status",
          "text": "    LPDIRECTSOUNDBUFFER *buffer;\n    int result;\n    DWORD status;"
        },
        {
          "name": "status-buffer-result",
          "text": "    DWORD status;\n    LPDIRECTSOUNDBUFFER *buffer;\n    int result;"
        },
        {
          "name": "status-result-buffer",
          "text": "    DWORD status;\n    int result;\n    LPDIRECTSOUNDBUFFER *buffer;"
        },
        {
          "name": "result-buffer-status",
          "text": "    int result;\n    LPDIRECTSOUNDBUFFER *buffer;\n    DWORD status;"
        },
        {
          "name": "result-status-buffer",
          "text": "    int result;\n    DWORD status;\n    LPDIRECTSOUNDBUFFER *buffer;"
        }
      ]
    },
    {
      "name": "stream-result-lifetime",
      "find": "    result = 0;\n    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    }\n\n",
      "replacements": [
        {
          "name": "result-after-streaming",
          "text": "    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    }\n    result = 0;\n\n"
        },
        {
          "name": "resident-label-result",
          "text": "    if (entry->vorbis_stream == 0) {\n        goto resident_playback;\n    }\nstream_playback:\n    sfx_entry_seek(entry, 0);\n    music_stream_fill(entry);\n    music_stream_fill(entry);\n    music_stream_fill(entry);\n    (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n    return 0;\n\nresident_playback:\n    result = 0;\n\n"
        },
        {
          "name": "resident-else-streaming",
          "text": "    if (entry->vorbis_stream == 0) {\n        result = 0;\n    } else {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    }\n\n"
        },
        {
          "name": "streaming-else-resident",
          "text": "    if (entry->vorbis_stream != 0) {\nstream_playback:\n        sfx_entry_seek(entry, 0);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        music_stream_fill(entry);\n        (*buffer)->Play(0, 0, DSBPLAY_LOOPING);\n        return 0;\n    } else {\n        result = 0;\n    }\n\n"
        }
      ]
    }
  ]
}
