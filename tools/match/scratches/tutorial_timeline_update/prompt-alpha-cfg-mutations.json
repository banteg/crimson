{
  "schema": 1,
  "sites": [
    {
      "name": "prompt-alpha-cfg",
      "find": "    {\n        int prompt_transition = tutorial_stage_transition_timer;\n        int stage_timer = tutorial_stage_timer;\n        float prompt_alpha;\n        if (prompt_transition < 0) {\n            if (prompt_transition < -1) {\n                prompt_alpha = (float)-prompt_transition * 0.001f;\n            } else {\n                prompt_alpha = 1.0f;\n            }\n        } else {\n            prompt_alpha =\n                (float)tutorial_stage_transition_timer * 0.001f;\n        }\n\n        stage = tutorial_stage_index;\n        if (prompt_alpha >= 1.0f) {\n            if (stage == 5\n                && stage_timer > 5000\n                && prompt_transition >= -1) {\n                prompt_alpha = 1.0f\n                    - (float)(stage_timer - 5000) * 0.001f;\n            }\n        }\n        if (stage == 5 && stage_timer > 6000) {\n            prompt_alpha = 0.0f;\n        }\n        if (prompt_alpha > 1.0f) {\n            prompt_alpha = 1.0f;\n        } else if (prompt_alpha < 0.0f) {\n            prompt_alpha = 0.0f;\n        }\n\n        if (stage >= 0 && (stage != 6 || perk_pending_count > 0)) {\n            tutorial_prompt_dialog(stage_text[stage], prompt_alpha, stage == 8);\n        }\n    }\n",
      "replacements": [
        {
          "name": "duplicate-stage-load-by-alpha-arm",
          "text": "    {\n        int prompt_transition = tutorial_stage_transition_timer;\n        int stage_timer = tutorial_stage_timer;\n        float prompt_alpha;\n        if (prompt_transition < 0) {\n            if (prompt_transition < -1) {\n                prompt_alpha = (float)-prompt_transition * 0.001f;\n            } else {\n                prompt_alpha = 1.0f;\n            }\n        } else {\n            prompt_alpha =\n                (float)tutorial_stage_transition_timer * 0.001f;\n        }\n\n        if (prompt_alpha >= 1.0f) {\n            stage = tutorial_stage_index;\n            if (stage == 5\n                && stage_timer > 5000\n                && prompt_transition >= -1) {\n                prompt_alpha = 1.0f\n                    - (float)(stage_timer - 5000) * 0.001f;\n            }\n        } else {\n            stage = tutorial_stage_index;\n        }\n        if (stage == 5 && stage_timer > 6000) {\n            prompt_alpha = 0.0f;\n        }\n        if (prompt_alpha > 1.0f) {\n            prompt_alpha = 1.0f;\n        } else if (prompt_alpha < 0.0f) {\n            prompt_alpha = 0.0f;\n        }\n\n        if (stage >= 0 && (stage != 6 || perk_pending_count > 0)) {\n            tutorial_prompt_dialog(stage_text[stage], prompt_alpha, stage == 8);\n        }\n    }\n"
        },
        {
          "name": "duplicate-stage-load-less-than-arm",
          "text": "    {\n        int prompt_transition = tutorial_stage_transition_timer;\n        int stage_timer = tutorial_stage_timer;\n        float prompt_alpha;\n        if (prompt_transition < 0) {\n            if (prompt_transition < -1) {\n                prompt_alpha = (float)-prompt_transition * 0.001f;\n            } else {\n                prompt_alpha = 1.0f;\n            }\n        } else {\n            prompt_alpha =\n                (float)tutorial_stage_transition_timer * 0.001f;\n        }\n\n        if (prompt_alpha < 1.0f) {\n            stage = tutorial_stage_index;\n        } else {\n            stage = tutorial_stage_index;\n            if (stage == 5\n                && stage_timer > 5000\n                && prompt_transition >= -1) {\n                prompt_alpha = 1.0f\n                    - (float)(stage_timer - 5000) * 0.001f;\n            }\n        }\n        if (stage == 5 && stage_timer > 6000) {\n            prompt_alpha = 0.0f;\n        }\n        if (prompt_alpha > 1.0f) {\n            prompt_alpha = 1.0f;\n        } else if (prompt_alpha < 0.0f) {\n            prompt_alpha = 0.0f;\n        }\n\n        if (stage >= 0 && (stage != 6 || perk_pending_count > 0)) {\n            tutorial_prompt_dialog(stage_text[stage], prompt_alpha, stage == 8);\n        }\n    }\n"
        },
        {
          "name": "merge-stage-five-gate",
          "text": "    {\n        int prompt_transition = tutorial_stage_transition_timer;\n        int stage_timer = tutorial_stage_timer;\n        float prompt_alpha;\n        if (prompt_transition < 0) {\n            if (prompt_transition < -1) {\n                prompt_alpha = (float)-prompt_transition * 0.001f;\n            } else {\n                prompt_alpha = 1.0f;\n            }\n        } else {\n            prompt_alpha =\n                (float)tutorial_stage_transition_timer * 0.001f;\n        }\n\n        stage = tutorial_stage_index;\n        if (stage == 5) {\n            if (prompt_alpha >= 1.0f\n                && stage_timer > 5000\n                && prompt_transition >= -1) {\n                prompt_alpha = 1.0f\n                    - (float)(stage_timer - 5000) * 0.001f;\n            }\n            if (stage_timer > 6000) {\n                prompt_alpha = 0.0f;\n            }\n        }\n        if (prompt_alpha > 1.0f) {\n            prompt_alpha = 1.0f;\n        } else if (prompt_alpha < 0.0f) {\n            prompt_alpha = 0.0f;\n        }\n\n        if (stage >= 0 && (stage != 6 || perk_pending_count > 0)) {\n            tutorial_prompt_dialog(stage_text[stage], prompt_alpha, stage == 8);\n        }\n    }\n"
        },
        {
          "name": "flatten-negative-transition",
          "text": "    {\n        int prompt_transition = tutorial_stage_transition_timer;\n        int stage_timer = tutorial_stage_timer;\n        float prompt_alpha;\n        if (prompt_transition < -1) {\n            prompt_alpha = (float)-prompt_transition * 0.001f;\n        } else if (prompt_transition < 0) {\n            prompt_alpha = 1.0f;\n        } else {\n            prompt_alpha =\n                (float)tutorial_stage_transition_timer * 0.001f;\n        }\n\n        stage = tutorial_stage_index;\n        if (prompt_alpha >= 1.0f) {\n            if (stage == 5\n                && stage_timer > 5000\n                && prompt_transition >= -1) {\n                prompt_alpha = 1.0f\n                    - (float)(stage_timer - 5000) * 0.001f;\n            }\n        }\n        if (stage == 5 && stage_timer > 6000) {\n            prompt_alpha = 0.0f;\n        }\n        if (prompt_alpha > 1.0f) {\n            prompt_alpha = 1.0f;\n        } else if (prompt_alpha < 0.0f) {\n            prompt_alpha = 0.0f;\n        }\n\n        if (stage >= 0 && (stage != 6 || perk_pending_count > 0)) {\n            tutorial_prompt_dialog(stage_text[stage], prompt_alpha, stage == 8);\n        }\n    }\n"
        },
        {
          "name": "neutral-sentinel-first",
          "text": "    {\n        int prompt_transition = tutorial_stage_transition_timer;\n        int stage_timer = tutorial_stage_timer;\n        float prompt_alpha;\n        if (prompt_transition == -1) {\n            prompt_alpha = 1.0f;\n        } else if (prompt_transition < -1) {\n            prompt_alpha = (float)-prompt_transition * 0.001f;\n        } else {\n            prompt_alpha =\n                (float)tutorial_stage_transition_timer * 0.001f;\n        }\n\n        stage = tutorial_stage_index;\n        if (prompt_alpha >= 1.0f) {\n            if (stage == 5\n                && stage_timer > 5000\n                && prompt_transition >= -1) {\n                prompt_alpha = 1.0f\n                    - (float)(stage_timer - 5000) * 0.001f;\n            }\n        }\n        if (stage == 5 && stage_timer > 6000) {\n            prompt_alpha = 0.0f;\n        }\n        if (prompt_alpha > 1.0f) {\n            prompt_alpha = 1.0f;\n        } else if (prompt_alpha < 0.0f) {\n            prompt_alpha = 0.0f;\n        }\n\n        if (stage >= 0 && (stage != 6 || perk_pending_count > 0)) {\n            tutorial_prompt_dialog(stage_text[stage], prompt_alpha, stage == 8);\n        }\n    }\n"
        },
        {
          "name": "positive-transition-first",
          "text": "    {\n        int prompt_transition = tutorial_stage_transition_timer;\n        int stage_timer = tutorial_stage_timer;\n        float prompt_alpha;\n        if (prompt_transition >= 0) {\n            prompt_alpha =\n                (float)tutorial_stage_transition_timer * 0.001f;\n        } else if (prompt_transition < -1) {\n            prompt_alpha = (float)-prompt_transition * 0.001f;\n        } else {\n            prompt_alpha = 1.0f;\n        }\n\n        stage = tutorial_stage_index;\n        if (prompt_alpha >= 1.0f) {\n            if (stage == 5\n                && stage_timer > 5000\n                && prompt_transition >= -1) {\n                prompt_alpha = 1.0f\n                    - (float)(stage_timer - 5000) * 0.001f;\n            }\n        }\n        if (stage == 5 && stage_timer > 6000) {\n            prompt_alpha = 0.0f;\n        }\n        if (prompt_alpha > 1.0f) {\n            prompt_alpha = 1.0f;\n        } else if (prompt_alpha < 0.0f) {\n            prompt_alpha = 0.0f;\n        }\n\n        if (stage >= 0 && (stage != 6 || perk_pending_count > 0)) {\n            tutorial_prompt_dialog(stage_text[stage], prompt_alpha, stage == 8);\n        }\n    }\n"
        }
      ]
    }
  ]
}
