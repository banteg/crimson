# vorbis_mem_tell

Native target: `crimsonland.exe` at `0x0041dda0` (8 bytes).

The tell hook returns the signed `long` cursor stored four bytes before the
inline datasource. Natural C++ matches all three instructions.
