# audio_resume_all

Resumes eligible music channels, optionally logs under `cv_verbose`, clears the
global suspension latch, and returns success.

Exact 14/14-instruction match with all seven native references aligned.
