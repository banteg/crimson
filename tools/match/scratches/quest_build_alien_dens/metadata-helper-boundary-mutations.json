{
  "schema": 1,
  "sites": [
    {
      "name": "entry-zero-metadata",
      "find": "    spawns[0].template_id = spawn_template_id;\n    spawns[0].trigger_time_ms = trigger_time_ms;\n    spawns[0].count = one;\n",
      "replacements": [
        {
          "name": "set-spawn",
          "text": "    spawns[0].set_spawn(\n        spawn_template_id, trigger_time_ms, one);\n"
        }
      ]
    },
    {
      "name": "entry-one-metadata",
      "find": "    entry_one->template_id = spawn_template_id;\n    entry_one->trigger_time_ms = trigger_time_ms;\n    entry_one->count = one;\n",
      "replacements": [
        {
          "name": "set-spawn",
          "text": "    entry_one->set_spawn(\n        spawn_template_id, trigger_time_ms, one);\n"
        }
      ]
    },
    {
      "name": "entry-two-metadata",
      "find": "    spawns[2].template_id = spawn_template_id;\n    spawns[2].trigger_time_ms = 23500;\n    spawns[2].count = config_blob.player_count;\n",
      "replacements": [
        {
          "name": "set-spawn",
          "text": "    spawns[2].set_spawn(\n        spawn_template_id, 23500, config_blob.player_count);\n"
        }
      ]
    },
    {
      "name": "entry-three-metadata",
      "find": "    entry_three->template_id = spawn_template_id;\n    entry_three->trigger_time_ms = trigger_time_ms;\n    entry_three->count = one;\n",
      "replacements": [
        {
          "name": "set-spawn",
          "text": "    entry_three->set_spawn(\n        spawn_template_id, trigger_time_ms, one);\n"
        }
      ]
    },
    {
      "name": "entry-four-metadata",
      "find": "    spawns[4].template_id = spawn_template_id;\n    spawns[4].trigger_time_ms = trigger_time_ms;\n    spawns[4].count = one;\n",
      "replacements": [
        {
          "name": "set-spawn",
          "text": "    spawns[4].set_spawn(\n        spawn_template_id, trigger_time_ms, one);\n"
        }
      ]
    }
  ]
}
