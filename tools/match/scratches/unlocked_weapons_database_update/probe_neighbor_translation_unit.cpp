// Native address order: weapons callback, its two local-static destructor
// thunks, perks callback, then its two destructor thunks. The guarded helper
// types let both independently recovered functions share their class identity.
#include "unlocked_database_translation_unit_probe.h"
