{
  "schema": 1,
  "sites": [
    {
      "name": "strdup-import-declaration",
      "find": "enum { D3DSGR_NO_CALIBRATION = 1 };",
      "replacements": [
        {
          "name": "dllimport-strdup",
          "text": "extern \"C\" __declspec(dllimport) char *__cdecl strdup(\n    const char *source);\n\nenum { D3DSGR_NO_CALIBRATION = 1 };"
        },
        {
          "name": "dllimport-underscore-strdup",
          "text": "extern \"C\" __declspec(dllimport) char *__cdecl _strdup(\n    const char *source);\n\nenum { D3DSGR_NO_CALIBRATION = 1 };"
        }
      ]
    },
    {
      "name": "strdup-call-spelling",
      "find": "            grim_config_values[id].words[3] =\n                (unsigned int)strdup(&grim_empty_string);\n            grim_lookup_blob_loaded = 0;\n        }\n\n        grim_lookup_blob_loaded = grim_lookup_blob_load(path);\n\n        char *stored = (char *)grim_config_values[id].words[3];\n        if (stored != 0) {\n            delete stored;\n        }\n        grim_config_values[id].words[3] = (unsigned int)strdup(path);",
      "replacements": [
        {
          "name": "underscore-strdup-calls",
          "text": "            grim_config_values[id].words[3] =\n                (unsigned int)_strdup(&grim_empty_string);\n            grim_lookup_blob_loaded = 0;\n        }\n\n        grim_lookup_blob_loaded = grim_lookup_blob_load(path);\n\n        char *stored = (char *)grim_config_values[id].words[3];\n        if (stored != 0) {\n            delete stored;\n        }\n        grim_config_values[id].words[3] = (unsigned int)_strdup(path);"
        }
      ]
    }
  ]
}
