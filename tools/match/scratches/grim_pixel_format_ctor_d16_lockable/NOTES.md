# `grim_pixel_format_ctor_d16_lockable`

Native target: `grim.dll` at `0x1001b0c5` (28 bytes).

This derived pixel-format constructor delegates `(desc, 16, 1)` to the common
format base and installs the D16_LOCKABLE vtable.
