# `grim_pixel_format_ctor_x8l8v8u8`

Native target: `grim.dll` at `0x1001b039` (28 bytes).

This derived pixel-format constructor delegates `(desc, 32, 2)` to the common
format base and installs the X8L8V8U8 vtable.
