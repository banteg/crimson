# ui_menu_main_click_controls

Native target: `crimsonland.exe` at `0x004473e0` (18 bytes).

The callback clears the transition direction and queues the controls-menu
state. Natural C++ matches all 3 instructions and references `2/0/0`.
