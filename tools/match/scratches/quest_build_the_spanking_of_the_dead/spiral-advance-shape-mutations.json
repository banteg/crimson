{
  "schema": 1,
  "sites": [
    {
      "name": "spiral-step-and-cursor-advance",
      "find": "        quest_entry_original_t *spawn = &spawns[step_index + 2];\n        spawn->pos = quest_vec2_t(\n            (float)cos(angle) * radius + 512.0f,\n            (float)sin(angle) * radius + 512.0f);\n        spawn->heading = angle;\n        spawn->template_id = SPAWN_ID_ZOMBIE_RANDOM_41;\n        spawn->trigger_time_ms = trigger_time_ms;\n        spawn->count = 1;\n\n        trigger_time_ms += 300;\n        ++step_index;\n",
      "replacements": [
        {
          "name": "step-after-entry-selection",
          "text": "        quest_entry_original_t *spawn = &spawns[step_index + 2];\n        ++step_index;\n        spawn->pos = quest_vec2_t(\n            (float)cos(angle) * radius + 512.0f,\n            (float)sin(angle) * radius + 512.0f);\n        spawn->heading = angle;\n        spawn->template_id = SPAWN_ID_ZOMBIE_RANDOM_41;\n        spawn->trigger_time_ms = trigger_time_ms;\n        spawn->count = 1;\n\n        trigger_time_ms += 300;\n"
        },
        {
          "name": "cursor-and-step-before-stores",
          "text": "        quest_entry_original_t *spawn = &spawns[step_index + 2];\n        ++spawn;\n        ++step_index;\n        spawn[-1].pos = quest_vec2_t(\n            (float)cos(angle) * radius + 512.0f,\n            (float)sin(angle) * radius + 512.0f);\n        spawn[-1].heading = angle;\n        spawn[-1].template_id = SPAWN_ID_ZOMBIE_RANDOM_41;\n        spawn[-1].trigger_time_ms = trigger_time_ms;\n        spawn[-1].count = 1;\n\n        trigger_time_ms += 300;\n"
        },
        {
          "name": "cursor-before-stores-step-at-tail",
          "text": "        quest_entry_original_t *spawn = &spawns[step_index + 2];\n        ++spawn;\n        spawn[-1].pos = quest_vec2_t(\n            (float)cos(angle) * radius + 512.0f,\n            (float)sin(angle) * radius + 512.0f);\n        spawn[-1].heading = angle;\n        spawn[-1].template_id = SPAWN_ID_ZOMBIE_RANDOM_41;\n        spawn[-1].trigger_time_ms = trigger_time_ms;\n        spawn[-1].count = 1;\n\n        trigger_time_ms += 300;\n        ++step_index;\n"
        }
      ]
    }
  ]
}
