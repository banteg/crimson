# `grim_pixel_format_ctor_l6v5u5`

Native target: `grim.dll` at `0x1001b01d` (28 bytes).

This derived pixel-format constructor delegates `(desc, 16, 2)` to the common
format base and installs the L6V5U5 vtable.
