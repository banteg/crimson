# buffer_reader_read_u16

Reads one native-endian 16-bit value and advances the unchecked global cursor.
The narrow return type explains why the native code only defines `AX`.

Exact 6/6-instruction match with all three native references aligned.
