{
  "schema": 1,
  "sites": [
    {
      "name": "vector-storage",
      "find": "    float x;\n    float y;",
      "replacements": [
        {
          "name": "original-union-storage",
          "text": "    union {\n        struct { float x, y; };\n        float v[2];\n    };"
        }
      ]
    },
    {
      "name": "vector-constructor",
      "find": "    creature_render_vec2_t(float x_value, float y_value)\n        : x(x_value), y(y_value) {}",
      "replacements": [
        {
          "name": "original-assignment-body",
          "text": "    creature_render_vec2_t(float x_value, float y_value)\n    {\n        x = x_value;\n        y = y_value;\n    }"
        }
      ]
    },
    {
      "name": "vector-add-operator",
      "find": "    creature_render_vec2_t operator+(\n        const creature_render_vec2_t &other) const\n    {\n        return creature_render_vec2_t(x + other.x, y + other.y);\n    }",
      "replacements": [
        {
          "name": "original-nonconst-operator",
          "text": "    creature_render_vec2_t operator+(\n        const creature_render_vec2_t &other)\n    {\n        return creature_render_vec2_t(x + other.x, y + other.y);\n    }"
        }
      ]
    },
    {
      "name": "vector-sub-operator",
      "find": "    creature_render_vec2_t operator-(\n        const creature_render_vec2_t &other) const\n    {\n        return creature_render_vec2_t(x - other.x, y - other.y);\n    }",
      "replacements": [
        {
          "name": "original-nonconst-operator",
          "text": "    creature_render_vec2_t operator-(\n        const creature_render_vec2_t &other)\n    {\n        return creature_render_vec2_t(x - other.x, y - other.y);\n    }"
        }
      ]
    },
    {
      "name": "color-constructor",
      "find": "    creature_render_color_t(\n        float r_value, float g_value, float b_value, float a_value)\n        : r(r_value), g(g_value), b(b_value), a(a_value) {}",
      "replacements": [
        {
          "name": "original-assignment-body",
          "text": "    creature_render_color_t(\n        float r_value, float g_value, float b_value, float a_value)\n    {\n        r = r_value;\n        g = g_value;\n        b = b_value;\n        a = a_value;\n    }"
        }
      ]
    }
  ]
}
