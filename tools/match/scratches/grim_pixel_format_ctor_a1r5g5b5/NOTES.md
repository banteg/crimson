# `grim_pixel_format_ctor_a1r5g5b5`

Native target: `grim.dll` at `0x1001a79d` (28 bytes).

This derived pixel-format constructor delegates `(desc, 16, 1)` to the common
format base and installs the A1R5G5B5 vtable.
