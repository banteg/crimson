# resource_pack_set

Validates a candidate pack by opening it for binary reads. A successful open
copies the path into the unchecked global buffer and enables pack lookup; a
failed open clears both the path and enabled flag.

Exact 37/37-instruction match with all seven native references aligned.
