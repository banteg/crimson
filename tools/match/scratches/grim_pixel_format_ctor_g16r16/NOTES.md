# `grim_pixel_format_ctor_g16r16`

Native target: `grim.dll` at `0x1001ac2e` (28 bytes).

This derived pixel-format constructor delegates `(desc, 32, 1)` to the common
format base and installs the G16R16 vtable.
