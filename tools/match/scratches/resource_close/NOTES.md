# resource_close

Closes the shared resource stream when non-null. The native helper deliberately
does not clear the global pointer afterward.

Exact 7/7-instruction match with both native references aligned.
