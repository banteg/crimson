# `grim_pixel_format_ctor_v8u8`

Native target: `grim.dll` at `0x1001b001` (28 bytes).

This derived pixel-format constructor delegates `(desc, 16, 2)` to the common
format base and installs the V8U8 vtable.
