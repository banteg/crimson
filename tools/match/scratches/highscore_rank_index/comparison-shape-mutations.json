{
  "schema": 1,
  "sites": [
    {
      "name": "rank-comparison-shape",
      "find": "int highscore_rank_index(void)\n{\n    int i;\n\n    if (config_game_mode == GAME_MODE_RUSH) {\n        for (i = 0; i < highscore_table_count; i++) {\n            if (survival_elapsed_ms >\n                (int)highscore_table[i].survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    if (config_game_mode == GAME_MODE_QUEST) {\n        for (i = 0; i < highscore_table_count; i++) {\n            if (survival_elapsed_ms <\n                (int)highscore_table[i].survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    for (i = 0; i < highscore_table_count; i++) {\n        if (highscore_score_xp > (int)highscore_table[i].score_xp) {\n            return i;\n        }\n    }\n    return highscore_table_count;\n}",
      "replacements": [
        {
          "name": "reversed-ranking-operands",
          "text": "int highscore_rank_index(void)\n{\n    int i;\n\n    if (config_game_mode == GAME_MODE_RUSH) {\n        for (i = 0; i < highscore_table_count; i++) {\n            if ((int)highscore_table[i].survival_elapsed_ms <\n                survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    if (config_game_mode == GAME_MODE_QUEST) {\n        for (i = 0; i < highscore_table_count; i++) {\n            if ((int)highscore_table[i].survival_elapsed_ms >\n                survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    for (i = 0; i < highscore_table_count; i++) {\n        if ((int)highscore_table[i].score_xp < highscore_score_xp) {\n            return i;\n        }\n    }\n    return highscore_table_count;\n}"
        },
        {
          "name": "per-iteration-record",
          "text": "int highscore_rank_index(void)\n{\n    int i;\n\n    if (config_game_mode == GAME_MODE_RUSH) {\n        for (i = 0; i < highscore_table_count; i++) {\n            highscore_record_t *record = &highscore_table[i];\n            if (survival_elapsed_ms > (int)record->survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    if (config_game_mode == GAME_MODE_QUEST) {\n        for (i = 0; i < highscore_table_count; i++) {\n            highscore_record_t *record = &highscore_table[i];\n            if (survival_elapsed_ms < (int)record->survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    for (i = 0; i < highscore_table_count; i++) {\n        highscore_record_t *record = &highscore_table[i];\n        if (highscore_score_xp > (int)record->score_xp) {\n            return i;\n        }\n    }\n    return highscore_table_count;\n}"
        },
        {
          "name": "per-iteration-value",
          "text": "int highscore_rank_index(void)\n{\n    int i;\n\n    if (config_game_mode == GAME_MODE_RUSH) {\n        for (i = 0; i < highscore_table_count; i++) {\n            int record_value = (int)highscore_table[i].survival_elapsed_ms;\n            if (survival_elapsed_ms > record_value) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    if (config_game_mode == GAME_MODE_QUEST) {\n        for (i = 0; i < highscore_table_count; i++) {\n            int record_value = (int)highscore_table[i].survival_elapsed_ms;\n            if (survival_elapsed_ms < record_value) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    for (i = 0; i < highscore_table_count; i++) {\n        int record_value = (int)highscore_table[i].score_xp;\n        if (highscore_score_xp > record_value) {\n            return i;\n        }\n    }\n    return highscore_table_count;\n}"
        },
        {
          "name": "per-arm-active-value",
          "text": "int highscore_rank_index(void)\n{\n    int i;\n\n    if (config_game_mode == GAME_MODE_RUSH) {\n        int active_value = survival_elapsed_ms;\n        for (i = 0; i < highscore_table_count; i++) {\n            if (active_value > (int)highscore_table[i].survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    if (config_game_mode == GAME_MODE_QUEST) {\n        int active_value = survival_elapsed_ms;\n        for (i = 0; i < highscore_table_count; i++) {\n            if (active_value < (int)highscore_table[i].survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    {\n        int active_value = highscore_score_xp;\n        for (i = 0; i < highscore_table_count; i++) {\n            if (active_value > (int)highscore_table[i].score_xp) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n}"
        },
        {
          "name": "active-value-reversed-operands",
          "text": "int highscore_rank_index(void)\n{\n    int i;\n\n    if (config_game_mode == GAME_MODE_RUSH) {\n        int active_value = survival_elapsed_ms;\n        for (i = 0; i < highscore_table_count; i++) {\n            if ((int)highscore_table[i].survival_elapsed_ms < active_value) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    if (config_game_mode == GAME_MODE_QUEST) {\n        int active_value = survival_elapsed_ms;\n        for (i = 0; i < highscore_table_count; i++) {\n            if ((int)highscore_table[i].survival_elapsed_ms > active_value) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    {\n        int active_value = highscore_score_xp;\n        for (i = 0; i < highscore_table_count; i++) {\n            if ((int)highscore_table[i].score_xp < active_value) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n}"
        }
      ]
    }
  ]
}
