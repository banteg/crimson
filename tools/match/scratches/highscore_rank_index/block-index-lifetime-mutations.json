{
  "schema": 1,
  "sites": [
    {
      "name": "rank-index-lifetime",
      "find": "int highscore_rank_index(void)\n{\n    int i;\n\n    if (config_game_mode == GAME_MODE_RUSH) {\n        for (i = 0; i < highscore_table_count; i++) {\n            if (survival_elapsed_ms >\n                (int)highscore_table[i].survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    if (config_game_mode == GAME_MODE_QUEST) {\n        for (i = 0; i < highscore_table_count; i++) {\n            if (survival_elapsed_ms <\n                (int)highscore_table[i].survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    for (i = 0; i < highscore_table_count; i++) {\n        if (highscore_score_xp > (int)highscore_table[i].score_xp) {\n            return i;\n        }\n    }\n    return highscore_table_count;\n}",
      "replacements": [
        {
          "name": "per-arm-index",
          "text": "int highscore_rank_index(void)\n{\n    if (config_game_mode == GAME_MODE_RUSH) {\n        int i;\n        for (i = 0; i < highscore_table_count; i++) {\n            if (survival_elapsed_ms >\n                (int)highscore_table[i].survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    if (config_game_mode == GAME_MODE_QUEST) {\n        int i;\n        for (i = 0; i < highscore_table_count; i++) {\n            if (survival_elapsed_ms <\n                (int)highscore_table[i].survival_elapsed_ms) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n\n    {\n        int i;\n        for (i = 0; i < highscore_table_count; i++) {\n            if (highscore_score_xp > (int)highscore_table[i].score_xp) {\n                return i;\n            }\n        }\n        return highscore_table_count;\n    }\n}"
        },
        {
          "name": "per-arm-initialized-index",
          "text": "int highscore_rank_index(void)\n{\n    if (config_game_mode == GAME_MODE_RUSH) {\n        int i = 0;\n        while (i < highscore_table_count) {\n            if (survival_elapsed_ms >\n                (int)highscore_table[i].survival_elapsed_ms) {\n                return i;\n            }\n            ++i;\n        }\n        return highscore_table_count;\n    }\n\n    if (config_game_mode == GAME_MODE_QUEST) {\n        int i = 0;\n        while (i < highscore_table_count) {\n            if (survival_elapsed_ms <\n                (int)highscore_table[i].survival_elapsed_ms) {\n                return i;\n            }\n            ++i;\n        }\n        return highscore_table_count;\n    }\n\n    {\n        int i = 0;\n        while (i < highscore_table_count) {\n            if (highscore_score_xp > (int)highscore_table[i].score_xp) {\n                return i;\n            }\n            ++i;\n        }\n        return highscore_table_count;\n    }\n}"
        },
        {
          "name": "per-arm-index-with-positive-count-guard",
          "text": "int highscore_rank_index(void)\n{\n    if (config_game_mode == GAME_MODE_RUSH) {\n        int i = 0;\n        if (highscore_table_count > 0) {\n            do {\n                if (survival_elapsed_ms >\n                    (int)highscore_table[i].survival_elapsed_ms) {\n                    return i;\n                }\n                ++i;\n            } while (i < highscore_table_count);\n        }\n        return highscore_table_count;\n    }\n\n    if (config_game_mode == GAME_MODE_QUEST) {\n        int i = 0;\n        if (highscore_table_count > 0) {\n            do {\n                if (survival_elapsed_ms <\n                    (int)highscore_table[i].survival_elapsed_ms) {\n                    return i;\n                }\n                ++i;\n            } while (i < highscore_table_count);\n        }\n        return highscore_table_count;\n    }\n\n    {\n        int i = 0;\n        if (highscore_table_count > 0) {\n            do {\n                if (highscore_score_xp >\n                    (int)highscore_table[i].score_xp) {\n                    return i;\n                }\n                ++i;\n            } while (i < highscore_table_count);\n        }\n        return highscore_table_count;\n    }\n}"
        }
      ]
    }
  ]
}
