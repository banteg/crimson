# console_cmdlist

Native target: `crimsonland.exe` at `0x00401370` (65 bytes).

The command walks the recovered command-node list, prints every name, and
prints the final count. Natural C++ matches all 24 instructions and references
`7/0/0`.
