# ui_callback_noop

Native target: `crimsonland.exe` at `0x00446140` (1 byte).

This is the empty placeholder callback used by menu/state flows. Its ordinary
empty body matches the native single `ret` instruction.
