# ui_menu_main_click_play_game

Native target: `crimsonland.exe` at `0x00447400` (18 bytes).

The callback clears the transition direction and queues the play-game menu.
Natural C++ matches all 3 instructions and references `2/0/0`.
