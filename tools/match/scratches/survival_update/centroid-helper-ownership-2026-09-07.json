{
  "schema": 1,
  "sites": [
    {
      "name": "centroid-helper-result-ownership",
      "find": "extern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n",
      "replacements": [
        {
          "name": "distance-inline-global",
          "text": "static inline float survival_recent_death_distance()\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy);\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_distance() < 16.0f\n                && player_state_table[0].health < 15.0f) {\n"
        },
        {
          "name": "qualifies-inline-global",
          "text": "static inline bool survival_recent_death_reward_ready()\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy) < 16.0f && player_state_table[0].health < 15.0f;\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_reward_ready()) {\n"
        },
        {
          "name": "distance-__forceinline-global",
          "text": "static __forceinline float survival_recent_death_distance()\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy);\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_distance() < 16.0f\n                && player_state_table[0].health < 15.0f) {\n"
        },
        {
          "name": "qualifies-__forceinline-global",
          "text": "static __forceinline bool survival_recent_death_reward_ready()\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy) < 16.0f && player_state_table[0].health < 15.0f;\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_reward_ready()) {\n"
        },
        {
          "name": "distance-inline-ref",
          "text": "static inline float survival_recent_death_distance(const vec2f_t &player_pos)\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_pos.x - pos.x;\n            float dy = player_pos.y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy);\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_distance(player_state_table[0].position) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n"
        },
        {
          "name": "qualifies-inline-ref",
          "text": "static inline bool survival_recent_death_reward_ready(const vec2f_t &player_pos)\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_pos.x - pos.x;\n            float dy = player_pos.y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy) < 16.0f && player_state_table[0].health < 15.0f;\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_reward_ready(player_state_table[0].position)) {\n"
        },
        {
          "name": "distance-__forceinline-ref",
          "text": "static __forceinline float survival_recent_death_distance(const vec2f_t &player_pos)\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_pos.x - pos.x;\n            float dy = player_pos.y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy);\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_distance(player_state_table[0].position) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n"
        },
        {
          "name": "qualifies-__forceinline-ref",
          "text": "static __forceinline bool survival_recent_death_reward_ready(const vec2f_t &player_pos)\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_pos.x - pos.x;\n            float dy = player_pos.y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy) < 16.0f && player_state_table[0].health < 15.0f;\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_reward_ready(player_state_table[0].position)) {\n"
        },
        {
          "name": "distance-inline-ptr",
          "text": "static inline float survival_recent_death_distance(const vec2f_t *player_pos)\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = (*player_pos).x - pos.x;\n            float dy = (*player_pos).y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy);\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_distance(&player_state_table[0].position) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n"
        },
        {
          "name": "qualifies-inline-ptr",
          "text": "static inline bool survival_recent_death_reward_ready(const vec2f_t *player_pos)\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = (*player_pos).x - pos.x;\n            float dy = (*player_pos).y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy) < 16.0f && player_state_table[0].health < 15.0f;\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_reward_ready(&player_state_table[0].position)) {\n"
        },
        {
          "name": "distance-__forceinline-ptr",
          "text": "static __forceinline float survival_recent_death_distance(const vec2f_t *player_pos)\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = (*player_pos).x - pos.x;\n            float dy = (*player_pos).y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy);\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_distance(&player_state_table[0].position) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n"
        },
        {
          "name": "qualifies-__forceinline-ptr",
          "text": "static __forceinline bool survival_recent_death_reward_ready(const vec2f_t *player_pos)\n{\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = (*player_pos).x - pos.x;\n            float dy = (*player_pos).y - pos.y;\n            return (float)sqrt(dx * dx + dy * dy) < 16.0f && player_state_table[0].health < 15.0f;\n}\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            if (survival_recent_death_reward_ready(&player_state_table[0].position)) {\n"
        }
      ]
    }
  ]
}
