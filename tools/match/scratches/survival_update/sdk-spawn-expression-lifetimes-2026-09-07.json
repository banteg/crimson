{
  "schema": 1,
  "sites": [
    {
      "name": "sdk-spawn-temporary-lifetimes",
      "find": "    float x;\n    float y;\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        survival_vec2_t pos;\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        pos.x = -164.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        pos.x = 1188.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        pos.x = 1088.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n",
      "replacements": [
        {
          "name": "sdk-type-control",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        survival_vec2_t pos;\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        pos.x = -164.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        pos.x = 1188.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        pos.x = 1088.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n"
        },
        {
          "name": "temp-unchanged",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)survival_vec2_t(-164.0f, 512.0f).v,\n            3.14159274f\n        );\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)survival_vec2_t(1188.0f, 512.0f).v,\n            3.14159274f\n        );\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)survival_vec2_t(1088.0f, 512.0f).v,\n            3.14159274f\n        );\n"
        },
        {
          "name": "temp-copy",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos = survival_recent_death_pos[0];\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)survival_vec2_t(-164.0f, 512.0f).v,\n            3.14159274f\n        );\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)survival_vec2_t(1188.0f, 512.0f).v,\n            3.14159274f\n        );\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)survival_vec2_t(1088.0f, 512.0f).v,\n            3.14159274f\n        );\n"
        },
        {
          "name": "temp-scale",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)survival_vec2_t(-164.0f, 512.0f).v,\n            3.14159274f\n        );\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)survival_vec2_t(1188.0f, 512.0f).v,\n            3.14159274f\n        );\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)survival_vec2_t(1088.0f, 512.0f).v,\n            3.14159274f\n        );\n"
        },
        {
          "name": "temp-copy-scale",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos = survival_recent_death_pos[0];\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)survival_vec2_t(-164.0f, 512.0f).v,\n            3.14159274f\n        );\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)survival_vec2_t(1188.0f, 512.0f).v,\n            3.14159274f\n        );\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)survival_vec2_t(1088.0f, 512.0f).v,\n            3.14159274f\n        );\n"
        },
        {
          "name": "const-ref-unchanged",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        {\n        const survival_vec2_t &pos = survival_vec2_t(-164.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        {\n        const survival_vec2_t &pos = survival_vec2_t(1188.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        {\n        const survival_vec2_t &pos = survival_vec2_t(1088.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n"
        },
        {
          "name": "const-ref-copy",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos = survival_recent_death_pos[0];\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        {\n        const survival_vec2_t &pos = survival_vec2_t(-164.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        {\n        const survival_vec2_t &pos = survival_vec2_t(1188.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        {\n        const survival_vec2_t &pos = survival_vec2_t(1088.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n"
        },
        {
          "name": "const-ref-scale",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        {\n        const survival_vec2_t &pos = survival_vec2_t(-164.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        {\n        const survival_vec2_t &pos = survival_vec2_t(1188.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        {\n        const survival_vec2_t &pos = survival_vec2_t(1088.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n"
        },
        {
          "name": "const-ref-copy-scale",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos = survival_recent_death_pos[0];\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        {\n        const survival_vec2_t &pos = survival_vec2_t(-164.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        {\n        const survival_vec2_t &pos = survival_vec2_t(1188.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        {\n        const survival_vec2_t &pos = survival_vec2_t(1088.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n"
        },
        {
          "name": "each-local-unchanged",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        {\n        survival_vec2_t pos(-164.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        {\n        survival_vec2_t pos(1188.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        {\n        survival_vec2_t pos(1088.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n"
        },
        {
          "name": "each-local-copy",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos = survival_recent_death_pos[0];\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        {\n        survival_vec2_t pos(-164.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        {\n        survival_vec2_t pos(1188.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        {\n        survival_vec2_t pos(1088.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n"
        },
        {
          "name": "each-local-scale",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        {\n        survival_vec2_t pos(-164.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        {\n        survival_vec2_t pos(1188.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        {\n        survival_vec2_t pos(1088.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n"
        },
        {
          "name": "each-local-copy-scale",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos = survival_recent_death_pos[0];\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 1;\n        {\n        survival_vec2_t pos(-164.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        {\n        survival_vec2_t pos(1188.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_spawn_stage = 2;\n        {\n        survival_vec2_t pos(1088.0f, 512.0f);\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n"
        },
        {
          "name": "stage-local-unchanged",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_vec2_t pos;\n        survival_spawn_stage = 1;\n        pos.x = -164.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        pos.x = 1188.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_vec2_t pos;\n        survival_spawn_stage = 2;\n        pos.x = 1088.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n"
        },
        {
          "name": "stage-local-copy",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos = survival_recent_death_pos[0];\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos.x *= 0.333333343f;\n            pos.y *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_vec2_t pos;\n        survival_spawn_stage = 1;\n        pos.x = -164.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        pos.x = 1188.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_vec2_t pos;\n        survival_spawn_stage = 2;\n        pos.x = 1088.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n"
        },
        {
          "name": "stage-local-scale",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos;\n            pos.x = survival_recent_death_pos[0].x;\n            pos.y = survival_recent_death_pos[0].y;\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_vec2_t pos;\n        survival_spawn_stage = 1;\n        pos.x = -164.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        pos.x = 1188.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_vec2_t pos;\n        survival_spawn_stage = 2;\n        pos.x = 1088.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n"
        },
        {
          "name": "stage-local-copy-scale",
          "text": "    union {\n        struct { float x; float y; };\n        float v[2];\n    };\n    survival_vec2_t() {}\n    survival_vec2_t(float x_value, float y_value) : x(x_value), y(y_value) {}\n    survival_vec2_t operator+(const survival_vec2_t &other) const\n    { return survival_vec2_t(x + other.x, y + other.y); }\n    survival_vec2_t &operator*=(float factor)\n    { x *= factor; y *= factor; return *this; }\n\n    survival_vec2_t &operator+=(const survival_vec2_t &other)\n    {\n        x += other.x;\n        y += other.y;\n        return *this;\n    }\n};\n\nextern \"C\" unsigned char console_open_flag;\nextern \"C\" unsigned char survival_reward_fire_seen;\nextern \"C\" unsigned char survival_reward_handout_enabled;\nextern \"C\" int config_player_count;\nextern \"C\" int frame_dt_ms;\nextern \"C\" int survival_reward_weapon_guard_id;\nextern \"C\" int survival_recent_death_count;\nextern \"C\" int survival_spawn_cooldown;\nextern \"C\" int quest_spawn_timeline;\nextern \"C\" int demo_time_limit_ms;\nextern \"C\" survival_vec2_t survival_recent_death_pos[3];\n\nextern \"C\" void demo_mode_start(void);\n\nextern \"C\" void survival_update(void)\n{\n    if (console_open_flag) {\n        return;\n    }\n\n    quest_spawn_timeline += frame_dt_ms;\n    if (demo_mode_active) {\n        if (quest_spawn_timeline > demo_time_limit_ms) {\n            render_pass_mode = 0;\n            demo_mode_start();\n        }\n        return;\n    }\n\n    {\n        if (config_player_count == 1) {\n        if (!survival_reward_damage_seen\n            && !survival_reward_fire_seen\n            && survival_elapsed_ms > 64000\n            && survival_reward_handout_enabled) {\n            if (player_state_table[0].weapon_id == WEAPON_ID_PISTOL) {\n                weapon_assign_player(0, WEAPON_ID_SHRINKIFIER_5K);\n                survival_reward_weapon_guard_id = WEAPON_ID_SHRINKIFIER_5K;\n            }\n            survival_reward_handout_enabled = 0;\n            survival_reward_damage_seen = 1;\n            survival_reward_fire_seen = 1;\n        }\n\n        if (survival_recent_death_count == 3\n            && !survival_reward_fire_seen) {\n            survival_vec2_t pos = survival_recent_death_pos[0];\n            pos += survival_recent_death_pos[1];\n            pos += survival_recent_death_pos[2];\n            pos *= 0.333333343f;\n            float dx = player_state_table[0].position.x - pos.x;\n            float dy = player_state_table[0].position.y - pos.y;\n            if ((float)sqrt(dx * dx + dy * dy) < 16.0f\n                && player_state_table[0].health < 15.0f) {\n                weapon_assign_player(0, WEAPON_ID_BLADE_GUN);\n                survival_reward_weapon_guard_id = WEAPON_ID_BLADE_GUN;\n                survival_reward_fire_seen = 1;\n                survival_reward_handout_enabled = 0;\n            }\n        }\n        }\n\n        if (survival_spawn_stage == 0) {\n        if (player_state_table[0].level <= 4) {\n            goto update_wave_spawns;\n        }\n        survival_vec2_t pos;\n        survival_spawn_stage = 1;\n        pos.x = -164.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        pos.x = 1188.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_FORMATION_RING_ALIEN_8_12,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n        }\n\n        if (survival_spawn_stage == 1) {\n        if (player_state_table[0].level <= 8) {\n            goto update_wave_spawns;\n        }\n        survival_vec2_t pos;\n        survival_spawn_stage = 2;\n        pos.x = 1088.0f;\n        pos.y = 512.0f;\n        creature_spawn_template(\n            SPAWN_ID_ALIEN_CONST_RED_BOSS_2C,\n            (const vec2f_t *)&pos,\n            3.14159274f\n        );\n"
        }
      ]
    }
  ]
}
