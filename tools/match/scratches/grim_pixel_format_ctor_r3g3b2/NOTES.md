# `grim_pixel_format_ctor_r3g3b2`

Native target: `grim.dll` at `0x1001aaa6` (28 bytes).

This derived pixel-format constructor delegates `(desc, 8, 1)` to the common
format base and installs the R3G3B2 vtable.
