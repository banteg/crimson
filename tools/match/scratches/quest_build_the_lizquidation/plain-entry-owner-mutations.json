{
  "schema": 1,
  "sites": [
    {
      "name": "plain-entry-owner",
      "find": "extern \"C\" void quest_build_the_lizquidation(\n    quest_spawn_entry_t *entries, int *count)\n{\n    quest_spawn_builder_t builder((quest_entry_original_t *)entries);\n    int wave = 0;\n\n    for (; wave < 10; ++wave) {\n        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n\n        spawn->set_spawn(\n            SPAWN_ID_LIZARD_RANDOM_2E,\n            wave * 8000 + 1500,\n            wave + 6);\n        spawn->pos.x = (float)(terrain_texture_width + 64);\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        ++builder.count;\n\n        spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        builder.spawns[builder.count].set_spawn(\n            SPAWN_ID_LIZARD_RANDOM_2E,\n            wave * 8000 + 1500,\n            wave + 6);\n        ++builder.count;\n\n        if (wave == 4) {\n            builder.spawns[builder.count].pos.x =\n                (float)(terrain_texture_width + 128);\n            builder.spawns[builder.count].pos.y =\n                (float)(terrain_texture_width / 2);\n            builder.spawns[builder.count].set_spawn(\n                SPAWN_ID_ALIEN_DEADLY_FAST_2B, 1500, 2);\n            ++builder.count;\n        }\n    }\n\n    *count = builder.count;\n}",
      "replacements": [
        {
          "name": "spawns-entry-count",
          "text": "extern \"C\" void quest_build_the_lizquidation(\n    quest_spawn_entry_t *entries, int *count)\n{\n    quest_entry_original_t *spawns =\n        (quest_entry_original_t *)entries;\n    int entry_count = 0;\n    int wave = 0;\n\n    for (; wave < 10; ++wave) {\n        quest_entry_original_t *spawn = &spawns[entry_count];\n\n        spawn->set_spawn(\n            SPAWN_ID_LIZARD_RANDOM_2E,\n            wave * 8000 + 1500,\n            wave + 6);\n        spawn->pos.x = (float)(terrain_texture_width + 64);\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        ++entry_count;\n\n        spawn = &spawns[entry_count];\n        spawn->pos.x = -64.0f;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        spawns[entry_count].set_spawn(\n            SPAWN_ID_LIZARD_RANDOM_2E,\n            wave * 8000 + 1500,\n            wave + 6);\n        ++entry_count;\n\n        if (wave == 4) {\n            spawns[entry_count].pos.x =\n                (float)(terrain_texture_width + 128);\n            spawns[entry_count].pos.y =\n                (float)(terrain_texture_width / 2);\n            spawns[entry_count].set_spawn(\n                SPAWN_ID_ALIEN_DEADLY_FAST_2B, 1500, 2);\n            ++entry_count;\n        }\n    }\n\n    *count = entry_count;\n}"
        },
        {
          "name": "entry-count-before-spawns",
          "text": "extern \"C\" void quest_build_the_lizquidation(\n    quest_spawn_entry_t *entries, int *count)\n{\n    int entry_count = 0;\n    quest_entry_original_t *spawns =\n        (quest_entry_original_t *)entries;\n    int wave = 0;\n\n    for (; wave < 10; ++wave) {\n        quest_entry_original_t *spawn = &spawns[entry_count];\n\n        spawn->set_spawn(\n            SPAWN_ID_LIZARD_RANDOM_2E,\n            wave * 8000 + 1500,\n            wave + 6);\n        spawn->pos.x = (float)(terrain_texture_width + 64);\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        ++entry_count;\n\n        spawn = &spawns[entry_count];\n        spawn->pos.x = -64.0f;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        spawns[entry_count].set_spawn(\n            SPAWN_ID_LIZARD_RANDOM_2E,\n            wave * 8000 + 1500,\n            wave + 6);\n        ++entry_count;\n\n        if (wave == 4) {\n            spawns[entry_count].pos.x =\n                (float)(terrain_texture_width + 128);\n            spawns[entry_count].pos.y =\n                (float)(terrain_texture_width / 2);\n            spawns[entry_count].set_spawn(\n                SPAWN_ID_ALIEN_DEADLY_FAST_2B, 1500, 2);\n            ++entry_count;\n        }\n    }\n\n    *count = entry_count;\n}"
        }
      ]
    }
  ]
}
