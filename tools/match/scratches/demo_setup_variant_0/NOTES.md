# demo_setup_variant_0

Native target: `crimsonland.exe` at `0x00402ed0` (263 bytes).

This two-player demo builds alternating columns of AI7 timer spiders down the
left and right sides of the arena, places both players inside the corridor,
and equips weapon 11. The 4-second limit and fixed 80-unit row spacing explain
the compact attract-mode encounter shape.

The recovered loop and whole-vector player assignments match all 70 native
instructions, full prefix, with all ten references aligned.
