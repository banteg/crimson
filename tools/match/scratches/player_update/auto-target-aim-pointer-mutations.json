{
  "schema": 1,
  "sites": [
    {
      "name": "auto-target-aim-lifetime",
      "find": "    } else {\n        int target_index = player->auto_target;\n        movement_input.y =\n            creature_pool[target_index].position.y - player->aim.y;\n        movement_input.x =\n            creature_pool[target_index].position.x - player->aim.x;\n        scalar = (float)sqrt(\n            movement_input.y * movement_input.y\n            + movement_input.x * movement_input.x);\n        if (scalar >= 4.0f) {\n            D3DXVec2Normalize(&movement_input, &movement_input);\n            angle_step = (scalar * 6.0f) * frame_dt;\n            move_delta.x = movement_input.x * angle_step;\n            player->aim.x = player->aim.x + move_delta.x;\n            player->aim.y = player->aim.y + movement_input.y * angle_step;\n        } else {\n            player->aim.x = creature_pool[target_index].position.x;\n            player->aim.y = creature_pool[target_index].position.y;\n        }\n        if (scalar < 128.0f && creature_pool[target_index].health > 0.0f) {\n            auto_fire = true;\n        }\n    }",
      "replacements": [
        {
          "name": "typed-pointer-explicit-add",
          "text": "    } else {\n        int target_index = player->auto_target;\n        player_update_vec2_t *player_aim = &player->aim;\n        movement_input.y =\n            creature_pool[target_index].position.y - player_aim->y;\n        movement_input.x =\n            creature_pool[target_index].position.x - player_aim->x;\n        scalar = (float)sqrt(\n            movement_input.y * movement_input.y\n            + movement_input.x * movement_input.x);\n        if (scalar >= 4.0f) {\n            D3DXVec2Normalize(&movement_input, &movement_input);\n            angle_step = (scalar * 6.0f) * frame_dt;\n            move_delta.x = movement_input.x * angle_step;\n            player_aim->x = player_aim->x + move_delta.x;\n            player_aim->y = player_aim->y + movement_input.y * angle_step;\n        } else {\n            player_aim->x = creature_pool[target_index].position.x;\n            player_aim->y = creature_pool[target_index].position.y;\n        }\n        if (scalar < 128.0f && creature_pool[target_index].health > 0.0f) {\n            auto_fire = true;\n        }\n    }"
        },
        {
          "name": "typed-pointer-compound-add",
          "text": "    } else {\n        int target_index = player->auto_target;\n        player_update_vec2_t *player_aim = &player->aim;\n        movement_input.y =\n            creature_pool[target_index].position.y - player_aim->y;\n        movement_input.x =\n            creature_pool[target_index].position.x - player_aim->x;\n        scalar = (float)sqrt(\n            movement_input.y * movement_input.y\n            + movement_input.x * movement_input.x);\n        if (scalar >= 4.0f) {\n            D3DXVec2Normalize(&movement_input, &movement_input);\n            angle_step = (scalar * 6.0f) * frame_dt;\n            move_delta.x = movement_input.x * angle_step;\n            player_aim->x += move_delta.x;\n            player_aim->y += movement_input.y * angle_step;\n        } else {\n            player_aim->x = creature_pool[target_index].position.x;\n            player_aim->y = creature_pool[target_index].position.y;\n        }\n        if (scalar < 128.0f && creature_pool[target_index].health > 0.0f) {\n            auto_fire = true;\n        }\n    }"
        },
        {
          "name": "vec2-pointer-explicit-add",
          "text": "    } else {\n        int target_index = player->auto_target;\n        vec2f_t *player_aim = &player->aim;\n        movement_input.y =\n            creature_pool[target_index].position.y - player_aim->y;\n        movement_input.x =\n            creature_pool[target_index].position.x - player_aim->x;\n        scalar = (float)sqrt(\n            movement_input.y * movement_input.y\n            + movement_input.x * movement_input.x);\n        if (scalar >= 4.0f) {\n            D3DXVec2Normalize(&movement_input, &movement_input);\n            angle_step = (scalar * 6.0f) * frame_dt;\n            move_delta.x = movement_input.x * angle_step;\n            player_aim->x = player_aim->x + move_delta.x;\n            player_aim->y = player_aim->y + movement_input.y * angle_step;\n        } else {\n            player_aim->x = creature_pool[target_index].position.x;\n            player_aim->y = creature_pool[target_index].position.y;\n        }\n        if (scalar < 128.0f && creature_pool[target_index].health > 0.0f) {\n            auto_fire = true;\n        }\n    }"
        },
        {
          "name": "typed-reference",
          "text": "    } else {\n        int target_index = player->auto_target;\n        player_update_vec2_t &player_aim = player->aim;\n        movement_input.y =\n            creature_pool[target_index].position.y - player_aim.y;\n        movement_input.x =\n            creature_pool[target_index].position.x - player_aim.x;\n        scalar = (float)sqrt(\n            movement_input.y * movement_input.y\n            + movement_input.x * movement_input.x);\n        if (scalar >= 4.0f) {\n            D3DXVec2Normalize(&movement_input, &movement_input);\n            angle_step = (scalar * 6.0f) * frame_dt;\n            move_delta.x = movement_input.x * angle_step;\n            player_aim.x = player_aim.x + move_delta.x;\n            player_aim.y = player_aim.y + movement_input.y * angle_step;\n        } else {\n            player_aim.x = creature_pool[target_index].position.x;\n            player_aim.y = creature_pool[target_index].position.y;\n        }\n        if (scalar < 128.0f && creature_pool[target_index].health > 0.0f) {\n            auto_fire = true;\n        }\n    }"
        }
      ]
    }
  ]
}
