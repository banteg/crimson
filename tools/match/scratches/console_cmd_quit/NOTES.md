# console_cmd_quit

Native target: `crimsonland.exe` at `0x401240` (8 bytes).

The registered console command sets the global quit-request byte. Natural C++
matches both native instructions and the single global reference exactly.
