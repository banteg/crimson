{
  "schema": 1,
  "sites": [
    {
      "name": "secondary-explosion-distance-lifetime",
      "find": "                        float dx = creature->position.x - secondary->position.x;\n                        float dy = creature->position.y - secondary->position.y;\n                        float distance =\n                            (float)sqrt(dx * dx + dy * dy);\n                        if (distance < radius) {\n                            vec2f_t impulse = {dx, dy};\n                            D3DXVec2Normalize(&impulse, &impulse);\n",
      "replacements": [
        {
          "name": "commuted-products",
          "text": "                        float dx = creature->position.x - secondary->position.x;\n                        float dy = creature->position.y - secondary->position.y;\n                        float distance =\n                            (float)sqrt(dy * dy + dx * dx);\n                        if (distance < radius) {\n                            vec2f_t impulse = {dx, dy};\n                            D3DXVec2Normalize(&impulse, &impulse);\n"
        },
        {
          "name": "named-squared-sum",
          "text": "                        float dx = creature->position.x - secondary->position.x;\n                        float dy = creature->position.y - secondary->position.y;\n                        float distance_squared = dx * dx + dy * dy;\n                        float distance = (float)sqrt(distance_squared);\n                        if (distance < radius) {\n                            vec2f_t impulse = {dx, dy};\n                            D3DXVec2Normalize(&impulse, &impulse);\n"
        },
        {
          "name": "named-products",
          "text": "                        float dx = creature->position.x - secondary->position.x;\n                        float dy = creature->position.y - secondary->position.y;\n                        float dx_squared = dx * dx;\n                        float dy_squared = dy * dy;\n                        float distance =\n                            (float)sqrt(dx_squared + dy_squared);\n                        if (distance < radius) {\n                            vec2f_t impulse = {dx, dy};\n                            D3DXVec2Normalize(&impulse, &impulse);\n"
        },
        {
          "name": "offset-vector-copied",
          "text": "                        vec2f_t offset = {\n                            creature->position.x - secondary->position.x,\n                            creature->position.y - secondary->position.y,\n                        };\n                        float distance = (float)sqrt(\n                            offset.x * offset.x + offset.y * offset.y);\n                        if (distance < radius) {\n                            vec2f_t impulse = offset;\n                            D3DXVec2Normalize(&impulse, &impulse);\n"
        },
        {
          "name": "impulse-reused-for-distance",
          "text": "                        vec2f_t impulse = {\n                            creature->position.x - secondary->position.x,\n                            creature->position.y - secondary->position.y,\n                        };\n                        float distance = (float)sqrt(\n                            impulse.x * impulse.x + impulse.y * impulse.y);\n                        if (distance < radius) {\n                            D3DXVec2Normalize(&impulse, &impulse);\n"
        },
        {
          "name": "named-dx-copy",
          "text": "                        float dx = creature->position.x - secondary->position.x;\n                        float dy = creature->position.y - secondary->position.y;\n                        float distance_dx = dx;\n                        float distance =\n                            (float)sqrt(distance_dx * dx + dy * dy);\n                        if (distance < radius) {\n                            vec2f_t impulse = {dx, dy};\n                            D3DXVec2Normalize(&impulse, &impulse);\n"
        },
        {
          "name": "named-dy-copy",
          "text": "                        float dx = creature->position.x - secondary->position.x;\n                        float dy = creature->position.y - secondary->position.y;\n                        float distance_dy = dy;\n                        float distance =\n                            (float)sqrt(dx * dx + distance_dy * dy);\n                        if (distance < radius) {\n                            vec2f_t impulse = {dx, dy};\n                            D3DXVec2Normalize(&impulse, &impulse);\n"
        }
      ]
    }
  ]
}
