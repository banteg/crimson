# grim_measure_text_width

Measures the maximum line width of a newline-delimited string using the small
font's byte-to-glyph map and per-glyph width table. A null pointer returns zero.

Matches all 45 native instructions and both masked references.
