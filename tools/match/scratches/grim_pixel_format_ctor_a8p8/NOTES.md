# `grim_pixel_format_ctor_a8p8`

Native target: `grim.dll` at `0x1001ae3c` (28 bytes).

This derived pixel-format constructor delegates `(desc, 16, 1)` to the common
format base and installs the A8P8 vtable.
