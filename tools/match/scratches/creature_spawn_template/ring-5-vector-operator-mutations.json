{
  "schema": 1,
  "sites": [
    {
      "name": "ring-vec2-operator-shape",
      "find": "    void set(float x_value, float y_value)\n    {\n        x = x_value;\n        y = y_value;\n    }",
      "replacements": [
        {
          "name": "lhs-first",
          "text": "    void set(float x_value, float y_value)\n    {\n        x = x_value;\n        y = y_value;\n    }\n\n    creature_spawn_vec2_t operator+(\n        const creature_spawn_vec2_t &other) const\n    {\n        creature_spawn_vec2_t result;\n        result.x = x + other.x;\n        result.y = y + other.y;\n        return result;\n    }"
        },
        {
          "name": "rhs-first",
          "text": "    void set(float x_value, float y_value)\n    {\n        x = x_value;\n        y = y_value;\n    }\n\n    creature_spawn_vec2_t operator+(\n        const creature_spawn_vec2_t &other) const\n    {\n        creature_spawn_vec2_t result;\n        result.x = other.x + x;\n        result.y = other.y + y;\n        return result;\n    }"
        }
      ]
    },
    {
      "name": "ring-5-vector-operator",
      "find": "            chain_position.set(\n                creature->target_offset.x + pos->x,\n                creature->target_offset.y + pos->y);\n            *(creature_spawn_vec2_t *)&creature->position = chain_position;",
      "replacements": [
        {
          "name": "target-plus-position",
          "text": "            chain_position =\n                *(creature_spawn_vec2_t *)&creature->target_offset +\n                *(const creature_spawn_vec2_t *)pos;\n            *(creature_spawn_vec2_t *)&creature->position = chain_position;"
        },
        {
          "name": "position-plus-target",
          "text": "            chain_position =\n                *(const creature_spawn_vec2_t *)pos +\n                *(creature_spawn_vec2_t *)&creature->target_offset;\n            *(creature_spawn_vec2_t *)&creature->position = chain_position;"
        }
      ]
    }
  ]
}
