{
  "schema": 1,
  "sites": [
    {
      "name": "arc-control-flow",
      "find": "    float step;\n    if (direct > wrapped) {\n        step = frame_dt * amount;\n        if (target < *angle) {\n            *angle = step * rate + *angle;\n            return amount;\n        }\n    } else {\n        step = frame_dt * amount;\n        if (target > *angle) {\n            *angle = step * rate + *angle;\n            return amount;\n        }\n    }\n    *angle = *angle - step * rate;\n",
      "replacements": [
        {
          "name": "explicit-second-arc-label",
          "text": "    float step;\n    if (direct <= wrapped) {\n        goto second_arc;\n    }\n    step = frame_dt * amount;\n    if (target < *angle) {\n        *angle = step * rate + *angle;\n        return amount;\n    }\n    goto subtract_step;\n\nsecond_arc:\n    step = frame_dt * amount;\n    if (target > *angle) {\n        *angle = step * rate + *angle;\n        return amount;\n    }\n\nsubtract_step:\n    *angle = *angle - step * rate;\n"
        },
        {
          "name": "explicit-first-arc-label",
          "text": "    float step;\n    if (direct > wrapped) {\n        goto first_arc;\n    }\n    step = frame_dt * amount;\n    if (target > *angle) {\n        *angle = step * rate + *angle;\n        return amount;\n    }\n    goto subtract_step;\n\nfirst_arc:\n    step = frame_dt * amount;\n    if (target < *angle) {\n        *angle = step * rate + *angle;\n        return amount;\n    }\n\nsubtract_step:\n    *angle = *angle - step * rate;\n"
        },
        {
          "name": "asymmetric-compound-product",
          "text": "    float step;\n    if (direct > wrapped) {\n        step = frame_dt * amount;\n        if (target < *angle) {\n            *angle = step * rate + *angle;\n            return amount;\n        }\n    } else {\n        step = frame_dt;\n        step *= amount;\n        if (target > *angle) {\n            *angle = step * rate + *angle;\n            return amount;\n        }\n    }\n    *angle = *angle - step * rate;\n"
        },
        {
          "name": "asymmetric-commuted-product",
          "text": "    float step;\n    if (direct > wrapped) {\n        step = frame_dt * amount;\n        if (target < *angle) {\n            *angle = step * rate + *angle;\n            return amount;\n        }\n    } else {\n        step = amount * frame_dt;\n        if (target > *angle) {\n            *angle = step * rate + *angle;\n            return amount;\n        }\n    }\n    *angle = *angle - step * rate;\n"
        }
      ]
    }
  ]
}
