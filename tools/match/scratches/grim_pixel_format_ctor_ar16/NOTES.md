# `grim_pixel_format_ctor_ar16`

Native target: `grim.dll` at `0x1001b3de` (28 bytes).

This derived pixel-format constructor delegates `(desc, 64, 1)` to the common
format base and installs the AR16 vtable.
