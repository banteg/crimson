{
  "schema": 1,
  "sites": [
    {
      "name": "accumulator-publication",
      "find": "    int entry_count = quest_spawn_count;\n    int total_creatures;\n    int last_spawn_time_ms;\n    quest_spawn_total_creatures = total_creatures = 0;\n    quest_spawn_last_time_ms = last_spawn_time_ms = 0;\n",
      "replacements": [
        {
          "name": "initialized-locals-then-publish",
          "text": "    int entry_count = quest_spawn_count;\n    int total_creatures = 0;\n    int last_spawn_time_ms = 0;\n    quest_spawn_total_creatures = total_creatures;\n    quest_spawn_last_time_ms = last_spawn_time_ms;\n"
        },
        {
          "name": "assigned-locals-then-publish",
          "text": "    int entry_count = quest_spawn_count;\n    int total_creatures;\n    int last_spawn_time_ms;\n    total_creatures = 0;\n    last_spawn_time_ms = 0;\n    quest_spawn_total_creatures = total_creatures;\n    quest_spawn_last_time_ms = last_spawn_time_ms;\n"
        },
        {
          "name": "publish-then-read-locals",
          "text": "    int entry_count = quest_spawn_count;\n    int total_creatures;\n    int last_spawn_time_ms;\n    quest_spawn_total_creatures = 0;\n    total_creatures = quest_spawn_total_creatures;\n    quest_spawn_last_time_ms = 0;\n    last_spawn_time_ms = quest_spawn_last_time_ms;\n"
        },
        {
          "name": "local-first-reversed-chains",
          "text": "    int entry_count = quest_spawn_count;\n    int total_creatures;\n    int last_spawn_time_ms;\n    total_creatures = quest_spawn_total_creatures = 0;\n    last_spawn_time_ms = quest_spawn_last_time_ms = 0;\n"
        },
        {
          "name": "swapped-local-declarations",
          "text": "    int entry_count = quest_spawn_count;\n    int last_spawn_time_ms;\n    int total_creatures;\n    quest_spawn_total_creatures = total_creatures = 0;\n    quest_spawn_last_time_ms = last_spawn_time_ms = 0;\n"
        },
        {
          "name": "direct-globals-then-local-initializers",
          "text": "    int entry_count = quest_spawn_count;\n    quest_spawn_total_creatures = 0;\n    quest_spawn_last_time_ms = 0;\n    int total_creatures = 0;\n    int last_spawn_time_ms = 0;\n"
        }
      ]
    },
    {
      "name": "hardcore-template-lifetime",
      "find": "        int template_id;\n        if (config_hardcore) {\n            if (count_cursor[0] > 1) {\n                template_id = count_cursor[-2];\n                if (template_id != 0x3c) {\n                    if (template_id == 0x2b) {\n                        count_cursor[0] += 2;\n                    } else {\n                        count_cursor[0] += 8;\n                    }\n                }\n            }\n        }\n",
      "replacements": [
        {
          "name": "nested-initialized-template",
          "text": "        if (config_hardcore) {\n            if (count_cursor[0] > 1) {\n                int template_id = count_cursor[-2];\n                if (template_id != 0x3c) {\n                    if (template_id == 0x2b) {\n                        count_cursor[0] += 2;\n                    } else {\n                        count_cursor[0] += 8;\n                    }\n                }\n            }\n        }\n"
        },
        {
          "name": "nested-assigned-template",
          "text": "        if (config_hardcore) {\n            if (count_cursor[0] > 1) {\n                int template_id;\n                template_id = count_cursor[-2];\n                if (template_id != 0x3c) {\n                    if (template_id == 0x2b) {\n                        count_cursor[0] += 2;\n                    } else {\n                        count_cursor[0] += 8;\n                    }\n                }\n            }\n        }\n"
        },
        {
          "name": "direct-template-field",
          "text": "        if (config_hardcore) {\n            if (count_cursor[0] > 1) {\n                if (count_cursor[-2] != 0x3c) {\n                    if (count_cursor[-2] == 0x2b) {\n                        count_cursor[0] += 2;\n                    } else {\n                        count_cursor[0] += 8;\n                    }\n                }\n            }\n        }\n"
        },
        {
          "name": "initialized-template-before-hardcore",
          "text": "        int template_id = count_cursor[-2];\n        if (config_hardcore) {\n            if (count_cursor[0] > 1) {\n                if (template_id != 0x3c) {\n                    if (template_id == 0x2b) {\n                        count_cursor[0] += 2;\n                    } else {\n                        count_cursor[0] += 8;\n                    }\n                }\n            }\n        }\n"
        }
      ]
    },
    {
      "name": "last-time-lifetime",
      "find": "        total_creatures += count_cursor[0];\n        int trigger_time_ms = count_cursor[-1];\n        if (last_spawn_time_ms < trigger_time_ms) {\n            last_spawn_time_ms = trigger_time_ms;\n        }\n",
      "replacements": [
        {
          "name": "trigger-declared-before-total",
          "text": "        int trigger_time_ms = count_cursor[-1];\n        total_creatures += count_cursor[0];\n        if (last_spawn_time_ms < trigger_time_ms) {\n            last_spawn_time_ms = trigger_time_ms;\n        }\n"
        },
        {
          "name": "trigger-assigned-after-total",
          "text": "        int trigger_time_ms;\n        total_creatures += count_cursor[0];\n        trigger_time_ms = count_cursor[-1];\n        if (last_spawn_time_ms < trigger_time_ms) {\n            last_spawn_time_ms = trigger_time_ms;\n        }\n"
        },
        {
          "name": "direct-trigger-field",
          "text": "        total_creatures += count_cursor[0];\n        if (last_spawn_time_ms < count_cursor[-1]) {\n            last_spawn_time_ms = count_cursor[-1];\n        }\n"
        }
      ]
    }
  ]
}
