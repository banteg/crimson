# audio_init_sfx

Selects the resident SFX pack, loads the native creature, weapon, impact, UI,
and blood asset paths into their concrete slots, preserves the unused gauss
slot and non-monotonic shock-family ordering, aliases three slots to the first
trooper pain sample, counts the contiguous successfully loaded entries, and
logs elapsed load time.
