# `grim_pixel_format_ctor_uyvy`

Native target: `grim.dll` at `0x1001b4dc` (24 bytes).

This derived constructor delegates the descriptor to the shared YUV-format
initializer and installs the UYVY vtable.
