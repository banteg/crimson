{
  "schema": 1,
  "sites": [
    {
      "name": "value-returning-add-wrapper",
      "find": "    float *vec2_add_out(float *dst, float *rhs);",
      "replacements": [
        {
          "name": "const-reference-rhs",
          "text": "    float *vec2_add_out(float *dst, float *rhs);\n\n    __forceinline controls_vec2_t add_out_value(\n        const controls_vec2_t &rhs)\n    {\n        controls_vec2_t result;\n        vec2_add_out((float *)&result, (float *)&rhs);\n        return result;\n    }"
        },
        {
          "name": "value-rhs",
          "text": "    float *vec2_add_out(float *dst, float *rhs);\n\n    __forceinline controls_vec2_t add_out_value(controls_vec2_t rhs)\n    {\n        controls_vec2_t result;\n        vec2_add_out((float *)&result, (float *)&rhs);\n        return result;\n    }"
        },
        {
          "name": "const-reference-named-result",
          "text": "    float *vec2_add_out(float *dst, float *rhs);\n\n    __forceinline controls_vec2_t add_out_value(\n        const controls_vec2_t &rhs)\n    {\n        controls_vec2_t result = controls_vec2_t();\n        vec2_add_out((float *)&result, (float *)&rhs);\n        return result;\n    }"
        }
      ]
    },
    {
      "name": "rebind-vector-add-expression",
      "find": "    controls_vec2_t line_offset(0.0f, 13.0f);\n    controls_vec2_t line_position;\n    grim_interface_ptr->grim_draw_rect_outline(\n        position.vec2_add_out(\n            (float *)&line_position,\n            (float *)&line_offset),\n        228.0f,\n        1.0f);\n\n    position.y += 18.0f;\n    position.x += 8.0f;\n    grim_interface_ptr->grim_set_color(1.0f, 1.0f, 1.0f, 0.7f);\n}\n\nstatic __forceinline void draw_rebind_row(\n    controls_vec2_t &position,\n    char *label,\n    int item_index)\n{\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        position.x, position.y, label);\n\n    controls_vec2_t item_offset(128.0f, 0.0f);\n    controls_vec2_t item_position;\n    ui_menu_item_update(\n        position.vec2_add_out(\n            (float *)&item_position,\n            (float *)&item_offset),\n        &controls_rebind_items[item_index]);",
      "replacements": [
        {
          "name": "copy-initialize-result",
          "text": "    controls_vec2_t line_position = position.add_out_value(\n        controls_vec2_t(0.0f, 13.0f));\n    grim_interface_ptr->grim_draw_rect_outline(\n        (float *)&line_position,\n        228.0f,\n        1.0f);\n\n    position.y += 18.0f;\n    position.x += 8.0f;\n    grim_interface_ptr->grim_set_color(1.0f, 1.0f, 1.0f, 0.7f);\n}\n\nstatic __forceinline void draw_rebind_row(\n    controls_vec2_t &position,\n    char *label,\n    int item_index)\n{\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        position.x, position.y, label);\n\n    controls_vec2_t item_position = position.add_out_value(\n        controls_vec2_t(128.0f, 0.0f));\n    ui_menu_item_update(\n        (float *)&item_position,\n        &controls_rebind_items[item_index]);"
        },
        {
          "name": "direct-construct-result",
          "text": "    controls_vec2_t line_position(position.add_out_value(\n        controls_vec2_t(0.0f, 13.0f)));\n    grim_interface_ptr->grim_draw_rect_outline(\n        (float *)&line_position,\n        228.0f,\n        1.0f);\n\n    position.y += 18.0f;\n    position.x += 8.0f;\n    grim_interface_ptr->grim_set_color(1.0f, 1.0f, 1.0f, 0.7f);\n}\n\nstatic __forceinline void draw_rebind_row(\n    controls_vec2_t &position,\n    char *label,\n    int item_index)\n{\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        position.x, position.y, label);\n\n    controls_vec2_t item_position(position.add_out_value(\n        controls_vec2_t(128.0f, 0.0f)));\n    ui_menu_item_update(\n        (float *)&item_position,\n        &controls_rebind_items[item_index]);"
        },
        {
          "name": "default-then-assign-result",
          "text": "    controls_vec2_t line_position;\n    line_position = position.add_out_value(\n        controls_vec2_t(0.0f, 13.0f));\n    grim_interface_ptr->grim_draw_rect_outline(\n        (float *)&line_position,\n        228.0f,\n        1.0f);\n\n    position.y += 18.0f;\n    position.x += 8.0f;\n    grim_interface_ptr->grim_set_color(1.0f, 1.0f, 1.0f, 0.7f);\n}\n\nstatic __forceinline void draw_rebind_row(\n    controls_vec2_t &position,\n    char *label,\n    int item_index)\n{\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        position.x, position.y, label);\n\n    controls_vec2_t item_position;\n    item_position = position.add_out_value(\n        controls_vec2_t(128.0f, 0.0f));\n    ui_menu_item_update(\n        (float *)&item_position,\n        &controls_rebind_items[item_index]);"
        }
      ]
    }
  ]
}
