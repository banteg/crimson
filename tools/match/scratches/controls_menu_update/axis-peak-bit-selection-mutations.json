{
  "schema": 1,
  "sites": [
    {
      "name": "axis-peak-bit-selection",
      "find": "static __forceinline float abs_float(float value)\n{\n    *(unsigned int *)&value &= 0x7fffffff;\n    return value;\n}\n\nstatic __forceinline void update_axis_peak(float &peak, int axis)\n{\n    float value = abs_float(grim_interface_ptr->grim_get_config_float(axis));\n    if (peak > value) {\n        value = peak;\n    }\n    peak = value;\n}\n",
      "replacements": [
        {
          "name": "punned-scalar-ternary",
          "text": "static __forceinline float abs_float(float value)\n{\n    unsigned int bits = *(unsigned int *)&value;\n    bits &= 0x7fffffff;\n    return *(float *)&bits;\n}\n\nstatic __forceinline void update_axis_peak(float &peak, int axis)\n{\n    float value = abs_float(grim_interface_ptr->grim_get_config_float(axis));\n    peak = peak > value ? peak : value;\n}\n"
        },
        {
          "name": "store-back-scalar-ternary",
          "text": "static __forceinline float abs_float(float value)\n{\n    unsigned int bits = *(unsigned int *)&value;\n    bits &= 0x7fffffff;\n    *(unsigned int *)&value = bits;\n    return value;\n}\n\nstatic __forceinline void update_axis_peak(float &peak, int axis)\n{\n    float value = abs_float(grim_interface_ptr->grim_get_config_float(axis));\n    peak = peak > value ? peak : value;\n}\n"
        },
        {
          "name": "direct-scalar-ternary",
          "text": "static __forceinline void update_axis_peak(float &peak, int axis)\n{\n    float value = grim_interface_ptr->grim_get_config_float(axis);\n    unsigned int bits = *(unsigned int *)&value;\n    bits &= 0x7fffffff;\n    *(unsigned int *)&value = bits;\n    peak = peak > value ? peak : value;\n}\n"
        },
        {
          "name": "punned-scalar-explicit-peak-branches",
          "text": "static __forceinline float abs_float(float value)\n{\n    unsigned int bits = *(unsigned int *)&value;\n    bits &= 0x7fffffff;\n    return *(float *)&bits;\n}\n\nstatic __forceinline void update_axis_peak(float &peak, int axis)\n{\n    float value = abs_float(grim_interface_ptr->grim_get_config_float(axis));\n    if (peak > value) {\n        peak = peak;\n    } else {\n        peak = value;\n    }\n}\n"
        },
        {
          "name": "punned-scalar-negated-comparison",
          "text": "static __forceinline float abs_float(float value)\n{\n    unsigned int bits = *(unsigned int *)&value;\n    bits &= 0x7fffffff;\n    return *(float *)&bits;\n}\n\nstatic __forceinline void update_axis_peak(float &peak, int axis)\n{\n    float value = abs_float(grim_interface_ptr->grim_get_config_float(axis));\n    if (!(peak > value)) {\n        peak = value;\n    }\n}\n"
        },
        {
          "name": "punned-scalar-staged-previous",
          "text": "static __forceinline float abs_float(float value)\n{\n    unsigned int bits = *(unsigned int *)&value;\n    bits &= 0x7fffffff;\n    return *(float *)&bits;\n}\n\nstatic __forceinline void update_axis_peak(float &peak, int axis)\n{\n    float previous = peak;\n    float value = abs_float(grim_interface_ptr->grim_get_config_float(axis));\n    peak = previous > value ? previous : value;\n}\n"
        }
      ]
    }
  ]
}
