{
  "schema": 1,
  "sites": [
    {
      "name": "east-phase-count",
      "find": "    quest_entry_original_t *spawn = builder.spawns;\n    while (x < 688) {\n        spawn->pos = quest_vec2_t((float)x, 256.0f);\n        spawn->set_spawn(\n            SPAWN_ID_DEN_SPIDER_WEAK_10,\n            trigger_time_ms,\n            1);\n        ++builder.count;\n        ++spawn;\n        x += 72;\n        trigger_time_ms += 900;\n    }\n",
      "replacements": [
        {
          "name": "reserve-six",
          "text": "    quest_entry_original_t *spawn = builder.spawns;\n    builder.count += 6;\n    while (x < 688) {\n        spawn->pos = quest_vec2_t((float)x, 256.0f);\n        spawn->set_spawn(\n            SPAWN_ID_DEN_SPIDER_WEAK_10,\n            trigger_time_ms,\n            1);\n        ++spawn;\n        x += 72;\n        trigger_time_ms += 900;\n    }\n"
        },
        {
          "name": "preadvance-direct",
          "text": "    quest_entry_original_t *spawn = builder.spawns;\n    while (x < 688) {\n        ++spawn;\n        spawn[-1].pos = quest_vec2_t((float)x, 256.0f);\n        spawn[-1].template_id = SPAWN_ID_DEN_SPIDER_WEAK_10;\n        spawn[-1].trigger_time_ms = trigger_time_ms;\n        spawn[-1].count = 1;\n        ++builder.count;\n        x += 72;\n        trigger_time_ms += 900;\n    }\n"
        },
        {
          "name": "reserve-six-preadvance",
          "text": "    quest_entry_original_t *spawn = builder.spawns;\n    builder.count += 6;\n    while (x < 688) {\n        ++spawn;\n        spawn[-1].pos = quest_vec2_t((float)x, 256.0f);\n        spawn[-1].template_id = SPAWN_ID_DEN_SPIDER_WEAK_10;\n        spawn[-1].trigger_time_ms = trigger_time_ms;\n        spawn[-1].count = 1;\n        x += 72;\n        trigger_time_ms += 900;\n    }\n"
        }
      ]
    },
    {
      "name": "south-phase-count",
      "find": "    int path_index = builder.count;\n    int y = 256;\n    trigger_time_ms = 5900;\n    spawn = &builder.spawns[builder.count];\n    while (y < 688) {\n        spawn->pos = quest_vec2_t(688.0f, (float)y);\n        spawn->set_spawn(\n            SPAWN_ID_DEN_SPIDER_WEAK_10,\n            trigger_time_ms,\n            1);\n        ++builder.count;\n        ++spawn;\n        ++path_index;\n        y += 72;\n        trigger_time_ms += 900;\n    }\n",
      "replacements": [
        {
          "name": "reserve-six",
          "text": "    int path_index = builder.count;\n    int y = 256;\n    trigger_time_ms = 5900;\n    spawn = &builder.spawns[builder.count];\n    builder.count += 6;\n    while (y < 688) {\n        spawn->pos = quest_vec2_t(688.0f, (float)y);\n        spawn->set_spawn(\n            SPAWN_ID_DEN_SPIDER_WEAK_10,\n            trigger_time_ms,\n            1);\n        ++spawn;\n        ++path_index;\n        y += 72;\n        trigger_time_ms += 900;\n    }\n"
        },
        {
          "name": "preadvance-direct",
          "text": "    int path_index = builder.count;\n    int y = 256;\n    trigger_time_ms = 5900;\n    spawn = &builder.spawns[builder.count];\n    while (y < 688) {\n        ++spawn;\n        spawn[-1].pos = quest_vec2_t(688.0f, (float)y);\n        spawn[-1].template_id = SPAWN_ID_DEN_SPIDER_WEAK_10;\n        spawn[-1].trigger_time_ms = trigger_time_ms;\n        spawn[-1].count = 1;\n        ++builder.count;\n        ++path_index;\n        y += 72;\n        trigger_time_ms += 900;\n    }\n"
        },
        {
          "name": "reserve-six-preadvance",
          "text": "    int path_index = builder.count;\n    int y = 256;\n    trigger_time_ms = 5900;\n    spawn = &builder.spawns[builder.count];\n    builder.count += 6;\n    while (y < 688) {\n        ++spawn;\n        spawn[-1].pos = quest_vec2_t(688.0f, (float)y);\n        spawn[-1].template_id = SPAWN_ID_DEN_SPIDER_WEAK_10;\n        spawn[-1].trigger_time_ms = trigger_time_ms;\n        spawn[-1].count = 1;\n        ++path_index;\n        y += 72;\n        trigger_time_ms += 900;\n    }\n"
        }
      ]
    }
  ]
}
