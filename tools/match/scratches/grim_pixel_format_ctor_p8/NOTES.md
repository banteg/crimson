# `grim_pixel_format_ctor_p8`

Native target: `grim.dll` at `0x1001ae74` (28 bytes).

This derived pixel-format constructor delegates `(desc, 8, 1)` to the common
format base and installs the P8 vtable.
