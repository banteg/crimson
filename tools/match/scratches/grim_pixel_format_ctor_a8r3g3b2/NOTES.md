# `grim_pixel_format_ctor_a8r3g3b2`

Native target: `grim.dll` at `0x1001aade` (28 bytes).

This derived pixel-format constructor delegates `(desc, 16, 1)` to the common
format base and installs the A8R3G3B2 vtable.
