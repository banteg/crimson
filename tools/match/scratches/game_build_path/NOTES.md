# game_build_path

Native target: `crimsonland.exe` at `0x00402bd0` (34 bytes).

The helper formats `game_base_path`, a backslash, and the supplied filename
into the shared path buffer and returns that buffer. Natural VC6 code matches
all 9 instructions and references `5/0/0`.
