# credits_line_set

This setter owns each credits-line string. It frees the previous allocation,
duplicates the replacement text, stores the line flags, and records the most
recent initialized index.

The recovered source is an exact 18/18-instruction match with all six native
references aligned.
