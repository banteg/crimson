{
  "schema": 1,
  "sites": [
    {
      "name": "font-atlas-inner-counter-identity",
      "find": "            int x = 0;\n            float *entry_v = row_v_cursor;\n            row_v_cursor += 32;\n            for (; x < 16; entry_v += 2, ++x) {\n                entry_v[-1] = (float)x * 0.0625f;\n",
      "replacements": [
        {
          "name": "column-counter",
          "text": "            int column = 0;\n            float *entry_v = row_v_cursor;\n            row_v_cursor += 32;\n            for (; column < 16; entry_v += 2, ++column) {\n                entry_v[-1] = (float)column * 0.0625f;\n"
        },
        {
          "name": "column-index-counter",
          "text": "            int column_index = 0;\n            float *entry_v = row_v_cursor;\n            row_v_cursor += 32;\n            for (; column_index < 16; entry_v += 2, ++column_index) {\n                entry_v[-1] = (float)column_index * 0.0625f;\n"
        },
        {
          "name": "unsigned-column-counter",
          "text": "            unsigned int column = 0;\n            float *entry_v = row_v_cursor;\n            row_v_cursor += 32;\n            for (; column < 16; entry_v += 2, ++column) {\n                entry_v[-1] = (float)column * 0.0625f;\n"
        },
        {
          "name": "long-column-counter",
          "text": "            long column = 0;\n            float *entry_v = row_v_cursor;\n            row_v_cursor += 32;\n            for (; column < 16; entry_v += 2, ++column) {\n                entry_v[-1] = (float)column * 0.0625f;\n"
        }
      ]
    }
  ]
}
