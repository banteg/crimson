# `grim_pixel_format_ctor_a4r4g4b4`

Native target: `grim.dll` at `0x1001aa8a` (28 bytes).

This derived pixel-format constructor delegates `(desc, 16, 1)` to the common
format base and installs the A4R4G4B4 vtable.
