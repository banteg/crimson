# console_cmd_set

Native target: `crimsonland.exe` at `0x00401510` (77 bytes).

The command requires exactly three tokens, otherwise printing the usage line.
It registers or updates the named cvar and reports the entry's stored name and
string value. Natural VC6 code matches all 22 instructions and references
`11/0/0`.
