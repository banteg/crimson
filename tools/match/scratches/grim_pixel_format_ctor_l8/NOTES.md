# `grim_pixel_format_ctor_l8`

Native target: `grim.dll` at `0x1001ae90` (28 bytes).

This derived pixel-format constructor delegates `(desc, 8, 1)` to the common
format base and installs the L8 vtable.
