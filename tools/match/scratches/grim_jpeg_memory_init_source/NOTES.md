# `grim_jpeg_memory_init_source`

Native target: `grim.dll` at `0x1003aa10`, 12 bytes.

This is the canonical recovered IJG memory-source initializer. It marks the
custom source manager as being at the start of its input and is compiled from
the same translation unit and `/O2 /G6 /W3 /MD` profile as the pinned native
provider.
