# `grim_pixel_format_ctor_v16u16`

Native target: `grim.dll` at `0x1001b071` (28 bytes).

This derived pixel-format constructor delegates `(desc, 32, 2)` to the common
format base and installs the V16U16 vtable.
