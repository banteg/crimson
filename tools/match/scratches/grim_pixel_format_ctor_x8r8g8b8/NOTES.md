# `grim_pixel_format_ctor_x8r8g8b8`

Native target: `grim.dll` at `0x1001a558` (28 bytes).

This derived pixel-format constructor delegates `(desc, 32, 1)` to the common
format base and installs the X8R8G8B8 vtable.
