# `grim_pixel_format_ctor_a2w10v10u10`

Native target: `grim.dll` at `0x1001b0a9` (28 bytes).

This derived pixel-format constructor delegates `(desc, 32, 2)` to the common
format base and installs the A2W10V10U10 vtable.
