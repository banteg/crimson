# `grim_pixel_format_ctor_r16`

Native target: `grim.dll` at `0x1001b3c2` (28 bytes).

This derived pixel-format constructor delegates `(desc, 48, 1)` to the common
format base and installs the R16 vtable.
