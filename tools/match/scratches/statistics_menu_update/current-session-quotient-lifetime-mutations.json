{
  "schema": 1,
  "sites": [
    {
      "name": "current-session-quotient-lifetime",
      "find": "    int session_seconds = (int)play_time_ms / 1000;\n    IGrim2D_cpp *session_renderer = grim_interface_ptr;\n    int session_minutes = session_seconds / 60;\n    int session_hours = session_minutes / 60;\n    int session_minute_part = session_minutes - session_hours * 60;\n    session_renderer->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minute_part,\n        session_seconds - session_minute_part * 60);",
      "replacements": [
        {
          "name": "hour-assigned-after-declaration",
          "text": "    int session_seconds = (int)play_time_ms / 1000;\n    IGrim2D_cpp *session_renderer = grim_interface_ptr;\n    int session_minutes = session_seconds / 60;\n    int session_hours;\n    session_hours = session_minutes / 60;\n    int session_minute_part = session_minutes - session_hours * 60;\n    session_renderer->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minute_part,\n        session_seconds - session_minute_part * 60);"
        },
        {
          "name": "hour-copy-compound-division",
          "text": "    int session_seconds = (int)play_time_ms / 1000;\n    IGrim2D_cpp *session_renderer = grim_interface_ptr;\n    int session_minutes = session_seconds / 60;\n    int session_hours = session_minutes;\n    session_hours /= 60;\n    int session_minute_part = session_minutes - session_hours * 60;\n    session_renderer->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minute_part,\n        session_seconds - session_minute_part * 60);"
        },
        {
          "name": "minute-copy-compound-division",
          "text": "    int session_seconds = (int)play_time_ms / 1000;\n    IGrim2D_cpp *session_renderer = grim_interface_ptr;\n    int session_minutes = session_seconds;\n    session_minutes /= 60;\n    int session_hours = session_minutes / 60;\n    int session_minute_part = session_minutes - session_hours * 60;\n    session_renderer->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minute_part,\n        session_seconds - session_minute_part * 60);"
        },
        {
          "name": "minute-and-hour-compound-division",
          "text": "    int session_seconds = (int)play_time_ms / 1000;\n    IGrim2D_cpp *session_renderer = grim_interface_ptr;\n    int session_minutes = session_seconds;\n    session_minutes /= 60;\n    int session_hours = session_minutes;\n    session_hours /= 60;\n    int session_minute_part = session_minutes - session_hours * 60;\n    session_renderer->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minute_part,\n        session_seconds - session_minute_part * 60);"
        },
        {
          "name": "minute-part-reuses-minutes",
          "text": "    int session_seconds = (int)play_time_ms / 1000;\n    IGrim2D_cpp *session_renderer = grim_interface_ptr;\n    int session_minutes = session_seconds / 60;\n    int session_hours = session_minutes / 60;\n    session_minutes -= session_hours * 60;\n    session_renderer->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minutes,\n        session_seconds - session_minutes * 60);"
        },
        {
          "name": "minute-part-modulo",
          "text": "    int session_seconds = (int)play_time_ms / 1000;\n    IGrim2D_cpp *session_renderer = grim_interface_ptr;\n    int session_minutes = session_seconds / 60;\n    int session_hours = session_minutes / 60;\n    int session_minute_part = session_minutes % 60;\n    session_renderer->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minute_part,\n        session_seconds - session_minute_part * 60);"
        },
        {
          "name": "named-second-part",
          "text": "    int session_seconds = (int)play_time_ms / 1000;\n    IGrim2D_cpp *session_renderer = grim_interface_ptr;\n    int session_minutes = session_seconds / 60;\n    int session_hours = session_minutes / 60;\n    int session_minute_part = session_minutes - session_hours * 60;\n    int session_second_part = session_seconds - session_minute_part * 60;\n    session_renderer->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minute_part,\n        session_second_part);"
        },
        {
          "name": "named-hour-minute-product",
          "text": "    int session_seconds = (int)play_time_ms / 1000;\n    IGrim2D_cpp *session_renderer = grim_interface_ptr;\n    int session_minutes = session_seconds / 60;\n    int session_hours = session_minutes / 60;\n    int session_hour_minutes = session_hours * 60;\n    int session_minute_part = session_minutes - session_hour_minutes;\n    session_renderer->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minute_part,\n        session_seconds - session_minute_part * 60);"
        },
        {
          "name": "all-results-assigned",
          "text": "    int session_seconds = (int)play_time_ms / 1000;\n    IGrim2D_cpp *session_renderer = grim_interface_ptr;\n    int session_minutes;\n    int session_hours;\n    int session_minute_part;\n    session_minutes = session_seconds / 60;\n    session_hours = session_minutes / 60;\n    session_minute_part = session_minutes - session_hours * 60;\n    session_renderer->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minute_part,\n        session_seconds - session_minute_part * 60);"
        }
      ]
    }
  ]
}
