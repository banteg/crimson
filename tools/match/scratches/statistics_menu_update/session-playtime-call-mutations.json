{
  "schema": 1,
  "sites": [
    {
      "name": "session-playtime-call",
      "find": "    int session_minutes = (int)game_sequence_id / 1000 / 60;\n    int session_hours = session_minutes / 60;\n    int session_minute_part = session_minutes - session_hours * 60;\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minute_part,\n        (int)game_sequence_id / 1000 - session_minute_part * 60);",
      "replacements": [
        {
          "name": "named-minutes-inline-parts-post-hours",
          "text": "    int session_minutes = (int)game_sequence_id / 1000 / 60;\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_minutes / 60,\n        session_minutes - session_minutes / 60 * 60,\n        (int)game_sequence_id / 1000\n            - (session_minutes - session_minutes / 60 * 60) * 60);\n    int session_hours = session_minutes / 60;"
        },
        {
          "name": "named-seconds-inline-parts-post-hours",
          "text": "    int session_seconds = (int)game_sequence_id / 1000;\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_seconds / 60 / 60,\n        session_seconds / 60 - session_seconds / 60 / 60 * 60,\n        session_seconds\n            - (session_seconds / 60\n               - session_seconds / 60 / 60 * 60) * 60);\n    int session_hours = session_seconds / 60 / 60;"
        },
        {
          "name": "named-minutes-modulo-parts-post-hours",
          "text": "    int session_minutes = (int)game_sequence_id / 1000 / 60;\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_minutes / 60,\n        session_minutes % 60,\n        (int)game_sequence_id / 1000 - session_minutes % 60 * 60);\n    int session_hours = session_minutes / 60;"
        },
        {
          "name": "named-seconds-modulo-parts-post-hours",
          "text": "    int session_seconds = (int)game_sequence_id / 1000;\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_seconds / 60 / 60,\n        session_seconds / 60 % 60,\n        session_seconds - session_seconds / 60 % 60 * 60);\n    int session_hours = session_seconds / 60 / 60;"
        },
        {
          "name": "assigned-hour-inline-independent-parts",
          "text": "    int session_minutes = (int)game_sequence_id / 1000 / 60;\n    int session_hours;\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours = session_minutes / 60,\n        session_minutes - session_minutes / 60 * 60,\n        (int)game_sequence_id / 1000\n            - (session_minutes - session_minutes / 60 * 60) * 60);"
        },
        {
          "name": "named-hour-inline-independent-parts",
          "text": "    int session_minutes = (int)game_sequence_id / 1000 / 60;\n    int session_hours = session_minutes / 60;\n    grim_interface_ptr->grim_draw_text_small_fmt(\n        xy.x - 38.0f,\n        xy.y + 230.0f,\n        \"played for %d hours %d minutes\",\n        session_hours,\n        session_minutes - session_minutes / 60 * 60,\n        (int)game_sequence_id / 1000\n            - (session_minutes - session_minutes / 60 * 60) * 60);"
        }
      ]
    }
  ]
}
