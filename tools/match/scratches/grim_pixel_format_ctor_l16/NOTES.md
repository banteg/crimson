# `grim_pixel_format_ctor_l16`

Native target: `grim.dll` at `0x1001b0e1` (28 bytes).

This derived pixel-format constructor delegates `(desc, 16, 1)` to the common
format base and installs the L16 vtable.
