# `grim_pixel_format_ctor_dxt1`

Native target: `grim.dll` at `0x1001b3fa` (24 bytes).

This derived constructor delegates the descriptor to the shared DXT-format
initializer and installs the DXT1 vtable.
