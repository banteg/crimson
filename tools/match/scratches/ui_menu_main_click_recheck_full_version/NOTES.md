# ui_menu_main_click_recheck_full_version

Native target: `crimsonland.exe` at `0x0044fca0` (8 bytes).

The callback sets the full-version recheck latch consumed by the main frame
loop. Natural VC6 code matches both instructions and its single reference.
