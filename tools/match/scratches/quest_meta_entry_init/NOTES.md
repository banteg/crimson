# quest_meta_entry_init

Native target: `crimsonland.exe` at `0x004121b0` (45 bytes).

The natural constructor establishes a two-minute default limit, terrain layers
0/1/2, starting weapon 1, and null builder/name/unlock fields.

It matches all thirteen native instructions, full prefix, with no references.
