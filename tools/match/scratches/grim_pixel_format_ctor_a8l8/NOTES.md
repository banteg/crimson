# `grim_pixel_format_ctor_a8l8`

Native target: `grim.dll` at `0x1001aeac` (28 bytes).

This derived pixel-format constructor delegates `(desc, 16, 1)` to the common
format base and installs the A8L8 vtable.
