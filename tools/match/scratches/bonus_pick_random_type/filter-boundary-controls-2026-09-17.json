{
  "schema": 1,
  "sites": [
    {
      "name": "filter-boundary",
      "find": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    } while (retries++ < 100);\n    return BONUS_ID_POINTS;\n}\n",
      "replacements": [
        {
          "name": "do-filter-rejections",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        do {\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            break;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                break;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    break;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        break;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        break;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            break;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            break;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            break;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            break;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            break;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n        } while (0);\n    } while (retries++ < 100);\n    return BONUS_ID_POINTS;\n}\n"
        },
        {
          "name": "do-for-retry-owner",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    for (; retries <= 100; ++retries) {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        do {\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            break;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                break;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    break;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        break;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        break;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            break;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            break;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            break;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            break;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            break;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n        } while (0);\n    }\n    return BONUS_ID_POINTS;\n}\n"
        },
        {
          "name": "do-decrement-retry-owner",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 101;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        do {\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            break;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                break;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    break;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        break;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        break;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            break;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            break;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            break;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            break;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            break;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n        } while (0);\n    } while (--retries);\n    return BONUS_ID_POINTS;\n}\n"
        },
        {
          "name": "switch-filter-rejections",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        switch (bonus_id) {\n        default:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            break;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                break;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    break;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        break;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        break;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            break;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            break;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            break;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            break;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            break;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n        }\n    } while (retries++ < 100);\n    return BONUS_ID_POINTS;\n}\n"
        },
        {
          "name": "switch-for-retry-owner",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    for (; retries <= 100; ++retries) {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        switch (bonus_id) {\n        default:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            break;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                break;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    break;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        break;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        break;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            break;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            break;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            break;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            break;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            break;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n        }\n    }\n    return BONUS_ID_POINTS;\n}\n"
        },
        {
          "name": "switch-decrement-retry-owner",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 101;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        switch (bonus_id) {\n        default:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            break;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                break;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    break;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        break;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        break;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    break;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            break;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            break;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            break;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            break;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            break;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n        }\n    } while (--retries);\n    return BONUS_ID_POINTS;\n}\n"
        },
        {
          "name": "filter-helper-bool-True",
          "text": "#include \"crimsonland_gameplay.h\"\n\nstatic inline bool bonus_is_allowed(bonus_id_t bonus_id, bool has_fire_bullets_drop)\n{\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            return false;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    return false;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                return false;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    return false;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        return false;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        return false;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    return false;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            return false;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            return false;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            return false;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            return false;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            return false;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return true;\n        }\n        return false;\n}\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (bonus_is_allowed(bonus_id, has_fire_bullets_drop)) {\n            return bonus_id;\n        }\n    } while (retries++ < 100);\n    return BONUS_ID_POINTS;\n}\n"
        },
        {
          "name": "filter-helper-bool-False",
          "text": "#include \"crimsonland_gameplay.h\"\n\nstatic inline bool bonus_is_rejected(bonus_id_t bonus_id, bool has_fire_bullets_drop)\n{\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            return true;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    return true;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                return true;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    return true;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        return true;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        return true;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    return true;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            return true;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            return true;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            return true;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            return true;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            return true;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return false;\n        }\n        return true;\n}\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (!bonus_is_rejected(bonus_id, has_fire_bullets_drop)) {\n            return bonus_id;\n        }\n    } while (retries++ < 100);\n    return BONUS_ID_POINTS;\n}\n"
        },
        {
          "name": "filter-helper-int-True",
          "text": "#include \"crimsonland_gameplay.h\"\n\nstatic inline int bonus_is_allowed(bonus_id_t bonus_id, bool has_fire_bullets_drop)\n{\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            return false;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    return false;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                return false;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    return false;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        return false;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        return false;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    return false;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            return false;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            return false;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            return false;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            return false;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            return false;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return true;\n        }\n        return false;\n}\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (bonus_is_allowed(bonus_id, has_fire_bullets_drop)) {\n            return bonus_id;\n        }\n    } while (retries++ < 100);\n    return BONUS_ID_POINTS;\n}\n"
        },
        {
          "name": "filter-helper-int-False",
          "text": "#include \"crimsonland_gameplay.h\"\n\nstatic inline int bonus_is_rejected(bonus_id_t bonus_id, bool has_fire_bullets_drop)\n{\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            return true;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    return true;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                return true;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    return true;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        return true;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        return true;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    return true;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            return true;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            return true;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            return true;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            return true;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            return true;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return false;\n        }\n        return true;\n}\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (!bonus_is_rejected(bonus_id, has_fire_bullets_drop)) {\n            return bonus_id;\n        }\n    } while (retries++ < 100);\n    return BONUS_ID_POINTS;\n}\n"
        },
        {
          "name": "short-circuit-secondary-helper",
          "text": "#include \"crimsonland_gameplay.h\"\n\nstatic inline bool quest_reject_bonus(bonus_id_t bonus_id)\n{\n    return quest_stage_major == 4\n        ? quest_stage_minor == 10 && (bonus_id == BONUS_ID_NUKE || bonus_id == BONUS_ID_FREEZE)\n        : quest_stage_major == 5 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE;\n}\n\nextern \"C\" bonus_id_t bonus_pick_random_type(void)\n{\n    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_reject_bonus(bonus_id)) {\n                continue;\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    } while (retries++ < 100);\n    return BONUS_ID_POINTS;\n}\n"
        }
      ]
    }
  ]
}
