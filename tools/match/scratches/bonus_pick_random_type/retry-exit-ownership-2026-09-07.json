{
  "schema": 1,
  "sites": [
    {
      "name": "retry-budget-and-exit-ownership",
      "find": "    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    } while (retries++ < 100);\n    return BONUS_ID_POINTS;\n",
      "replacements": [
        {
          "name": "for-le-early",
          "text": "    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    for (; retries <= 100; ++retries) {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    }\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "for-le-after-scan",
          "text": "    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    int retries = 0;\n    bonus_id_t bonus_id;\n    for (; retries <= 100; ++retries) {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    }\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "for-lt-early",
          "text": "    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    for (; retries < 101; ++retries) {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    }\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "for-lt-after-scan",
          "text": "    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    int retries = 0;\n    bonus_id_t bonus_id;\n    for (; retries < 101; ++retries) {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    }\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "while-le-early",
          "text": "    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    while (retries++ <= 100) {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    }\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "while-le-after-scan",
          "text": "    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    int retries = 0;\n    bonus_id_t bonus_id;\n    while (retries++ <= 100) {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    }\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "while-break-early",
          "text": "    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    while (true) {\n        if (retries++ > 100) break;\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    }\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "while-break-after-scan",
          "text": "    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    int retries = 0;\n    bonus_id_t bonus_id;\n    while (true) {\n        if (retries++ > 100) break;\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    }\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "for-forever-early",
          "text": "    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    for (;;) {\n        if (retries++ > 100) break;\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    }\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "for-forever-after-scan",
          "text": "    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    int retries = 0;\n    bonus_id_t bonus_id;\n    for (;;) {\n        if (retries++ > 100) break;\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\n    }\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "do-break-early",
          "text": "    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            goto retry;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    goto retry;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                goto retry;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    goto retry;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        goto retry;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        goto retry;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    goto retry;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            goto retry;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            goto retry;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            goto retry;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            goto retry;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            goto retry;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\nretry:\n        if (retries++ >= 100) break;\n    } while (true);\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "do-break-after-scan",
          "text": "    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    int retries = 0;\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            goto retry;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    goto retry;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                goto retry;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    goto retry;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        goto retry;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        goto retry;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    goto retry;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            goto retry;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            goto retry;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            goto retry;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            goto retry;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            goto retry;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            return bonus_id;\n        }\nretry:\n        if (retries++ >= 100) break;\n    } while (true);\n    return BONUS_ID_POINTS;\n"
        },
        {
          "name": "return-join-early",
          "text": "    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            goto retry;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    goto retry;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                goto retry;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    goto retry;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        goto retry;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        goto retry;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    goto retry;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            goto retry;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            goto retry;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            goto retry;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            goto retry;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            goto retry;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            break;\n        }\nretry:\n        if (retries++ >= 100) {\n            bonus_id = BONUS_ID_POINTS;\n            break;\n        }\n    } while (true);\n    return bonus_id;\n"
        },
        {
          "name": "return-join-after-scan",
          "text": "    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    int retries = 0;\n    bonus_id_t bonus_id;\n    do {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            goto retry;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    goto retry;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                goto retry;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    goto retry;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        goto retry;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        goto retry;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    goto retry;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            goto retry;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            goto retry;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            goto retry;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            goto retry;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            goto retry;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            break;\n        }\nretry:\n        if (retries++ >= 100) {\n            bonus_id = BONUS_ID_POINTS;\n            break;\n        }\n    } while (true);\n    return bonus_id;\n"
        },
        {
          "name": "for-return-join-early",
          "text": "    int retries = 0;\n    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    bonus_id_t bonus_id;\n    for (; retries <= 100; ++retries) {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            break;\n        }\n    }\n    return retries <= 100 ? bonus_id : BONUS_ID_POINTS;\n"
        },
        {
          "name": "for-return-join-after-scan",
          "text": "    bool has_fire_bullets_drop = false;\n    unsigned char *bonus_state = &bonus_pool[0].state;\n    do {\n        if (*(bonus_id_t *)(bonus_state - 4) == BONUS_ID_FIRE_BULLETS && !*bonus_state) {\n            has_fire_bullets_drop = true;\n        }\n        bonus_state += sizeof(bonus_entry_t);\n    } while ((int)bonus_state < (int)&bonus_pool[0x10].state);\n\n    int retries = 0;\n    bonus_id_t bonus_id;\n    for (; retries <= 100; ++retries) {\n        bonus_id = BONUS_ID_NONE;\n        int bucket = crt_rand() % 0xa2 + 1;\n        if (bucket <= 13) {\n            bonus_id = BONUS_ID_POINTS;\n        }\n        bucket -= 13;\n        if (bonus_id == BONUS_ID_NONE && bucket <= 1) {\n            bonus_id = BONUS_ID_ENERGIZER;\n        }\n        --bucket;\n\n        if (bonus_id == BONUS_ID_ENERGIZER) {\n            if ((crt_rand() & 0x3f) == 0) {\n                goto selected;\n            }\n            bonus_id = BONUS_ID_NONE;\n        } else if (bonus_id != BONUS_ID_NONE) {\n            goto selected;\n        }\n\n        {\n            bonus_id_t bucket_id = BONUS_ID_WEAPON;\n            while (bucket_id < 15) {\n                if (bucket <= 10) {\n                    bonus_id = bucket_id;\n                    break;\n                }\n                bucket -= 10;\n                bucket_id = (bonus_id_t)(bucket_id + 1);\n            }\n        }\n\nselected:\n        if (shock_chain_links_left > 0 && bonus_id == BONUS_ID_SHOCK_CHAIN) {\n            continue;\n        }\n\n        if (config_blob.game_mode == GAME_MODE_QUEST) {\n            if (config_blob.hardcore && quest_stage_major == 3) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            } else if (\n                quest_stage_major == 2 && quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE\n            ) {\n                continue;\n            }\n\n            if (config_blob.hardcore && quest_stage_major == 2) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_FREEZE) {\n                    continue;\n                }\n            } else if (quest_stage_major == 4) {\n                if (quest_stage_minor == 10) {\n                    if (bonus_id == BONUS_ID_NUKE) {\n                        continue;\n                    }\n                    if (bonus_id == BONUS_ID_FREEZE) {\n                        continue;\n                    }\n                }\n            } else if (quest_stage_major == 5) {\n                if (quest_stage_minor == 10 && bonus_id == BONUS_ID_NUKE) {\n                    continue;\n                }\n            }\n        }\n\n        if (bonus_freeze_timer > 0.0f && bonus_id == BONUS_ID_FREEZE) {\n            continue;\n        }\n        if (\n            (player_state_table[0].shield_timer > 0.0f || player_state_table[1].shield_timer > 0.0f)\n            && bonus_id == BONUS_ID_SHIELD\n        ) {\n            continue;\n        }\n        if (perk_count_get(perk_id_my_favourite_weapon) != 0 && bonus_id == BONUS_ID_WEAPON) {\n            continue;\n        }\n        if (perk_count_get(perk_id_death_clock) != 0 && bonus_id == BONUS_ID_MEDIKIT) {\n            continue;\n        }\n        if (bonus_id == BONUS_ID_WEAPON && has_fire_bullets_drop) {\n            continue;\n        }\n        if (bonus_meta_table[bonus_id].enabled) {\n            break;\n        }\n    }\n    return retries <= 100 ? bonus_id : BONUS_ID_POINTS;\n"
        }
      ]
    }
  ]
}
