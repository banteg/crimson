{
  "schema": 1,
  "sites": [
    {
      "name": "wave-trigger-cursor",
      "find": "    quest_entry_original_t *cursor = (quest_entry_original_t *)entries;\n    int trigger_time_ms = 2000;\n\n    for (int trigger_step_ms = 2000;\n         trigger_step_ms > 560;\n         trigger_step_ms -= 80) {\n        float angle = (float)(crt_rand() % 612) * 0.01f;\n        float angle_cos = (float)cos(angle);\n        float angle_sin = (float)sin(angle);\n        quest_entry_original_t *wave_entry = cursor;\n        cursor += 4;\n\n        for (int radius = 84; radius < 252; radius += 42) {\n            quest_vec2_t offset(\n                (float)radius * angle_cos,\n                (float)radius * angle_sin);\n            wave_entry->set(\n                quest_vec2_t(\n                    offset.x + 512.0f,\n                    offset.y + 512.0f),\n                SPAWN_ID_DEN_LIZARD_WEAK_SLOWER_0D,\n                trigger_time_ms,\n                1);\n            ++wave_entry;\n        }\n\n        trigger_time_ms += trigger_step_ms;\n    }",
      "replacements": [
        {
          "name": "typed-trigger-cursor-current-record",
          "text": "    int *wave_trigger =\n        &((quest_entry_original_t *)entries)->trigger_time_ms;\n    int trigger_time_ms = 2000;\n\n    for (int trigger_step_ms = 2000;\n         trigger_step_ms > 560;\n         trigger_step_ms -= 80) {\n        float angle = (float)(crt_rand() % 612) * 0.01f;\n        float angle_cos = (float)cos(angle);\n        float angle_sin = (float)sin(angle);\n        int *entry_trigger = wave_trigger;\n        wave_trigger += 24;\n\n        for (int radius = 84; radius < 252; radius += 42) {\n            quest_vec2_t offset(\n                (float)radius * angle_cos,\n                (float)radius * angle_sin);\n            quest_entry_original_t *wave_entry =\n                (quest_entry_original_t *)(entry_trigger - 4);\n            wave_entry->set(\n                quest_vec2_t(\n                    offset.x + 512.0f,\n                    offset.y + 512.0f),\n                SPAWN_ID_DEN_LIZARD_WEAK_SLOWER_0D,\n                trigger_time_ms,\n                1);\n            entry_trigger += 6;\n        }\n\n        trigger_time_ms += trigger_step_ms;\n    }"
        },
        {
          "name": "typed-trigger-cursor-next-record",
          "text": "    int *wave_trigger =\n        &((quest_entry_original_t *)entries)->trigger_time_ms;\n    int trigger_time_ms = 2000;\n\n    for (int trigger_step_ms = 2000;\n         trigger_step_ms > 560;\n         trigger_step_ms -= 80) {\n        float angle = (float)(crt_rand() % 612) * 0.01f;\n        float angle_cos = (float)cos(angle);\n        float angle_sin = (float)sin(angle);\n        int *entry_trigger = wave_trigger;\n        wave_trigger += 24;\n\n        for (int radius = 84; radius < 252; radius += 42) {\n            quest_vec2_t offset(\n                (float)radius * angle_cos,\n                (float)radius * angle_sin);\n            entry_trigger += 6;\n            quest_entry_original_t *wave_entry =\n                (quest_entry_original_t *)(entry_trigger - 10);\n            wave_entry->set(\n                quest_vec2_t(\n                    offset.x + 512.0f,\n                    offset.y + 512.0f),\n                SPAWN_ID_DEN_LIZARD_WEAK_SLOWER_0D,\n                trigger_time_ms,\n                1);\n        }\n\n        trigger_time_ms += trigger_step_ms;\n    }"
        },
        {
          "name": "direct-trigger-cursor-next-record",
          "text": "    int *wave_trigger =\n        &((quest_entry_original_t *)entries)->trigger_time_ms;\n    int trigger_time_ms = 2000;\n\n    for (int trigger_step_ms = 2000;\n         trigger_step_ms > 560;\n         trigger_step_ms -= 80) {\n        float angle = (float)(crt_rand() % 612) * 0.01f;\n        float angle_cos = (float)cos(angle);\n        float angle_sin = (float)sin(angle);\n        int *entry_trigger = wave_trigger;\n        wave_trigger += 24;\n\n        for (int radius = 84; radius < 252; radius += 42) {\n            quest_vec2_t offset(\n                (float)radius * angle_cos,\n                (float)radius * angle_sin);\n            quest_vec2_t position(\n                offset.x + 512.0f,\n                offset.y + 512.0f);\n            entry_trigger += 6;\n            ((float *)entry_trigger)[-10] = position.x;\n            ((float *)entry_trigger)[-9] = position.y;\n            entry_trigger[-7] = SPAWN_ID_DEN_LIZARD_WEAK_SLOWER_0D;\n            entry_trigger[-6] = trigger_time_ms;\n            entry_trigger[-5] = 1;\n        }\n\n        trigger_time_ms += trigger_step_ms;\n    }"
        },
        {
          "name": "byte-trigger-cursor-next-record",
          "text": "    unsigned char *wave_trigger =\n        (unsigned char *)&((quest_entry_original_t *)entries)->trigger_time_ms;\n    int trigger_time_ms = 2000;\n\n    for (int trigger_step_ms = 2000;\n         trigger_step_ms > 560;\n         trigger_step_ms -= 80) {\n        float angle = (float)(crt_rand() % 612) * 0.01f;\n        float angle_cos = (float)cos(angle);\n        float angle_sin = (float)sin(angle);\n        unsigned char *entry_trigger = wave_trigger;\n        wave_trigger += 96;\n\n        for (int radius = 84; radius < 252; radius += 42) {\n            quest_vec2_t offset(\n                (float)radius * angle_cos,\n                (float)radius * angle_sin);\n            entry_trigger += 24;\n            quest_entry_original_t *wave_entry =\n                (quest_entry_original_t *)(entry_trigger - 40);\n            wave_entry->set(\n                quest_vec2_t(\n                    offset.x + 512.0f,\n                    offset.y + 512.0f),\n                SPAWN_ID_DEN_LIZARD_WEAK_SLOWER_0D,\n                trigger_time_ms,\n                1);\n        }\n\n        trigger_time_ms += trigger_step_ms;\n    }"
        }
      ]
    }
  ]
}
