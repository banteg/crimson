# `grim_pixel_format_ctor_w11v11u10`

Native target: `grim.dll` at `0x1001b08d` (28 bytes).

This derived pixel-format constructor delegates `(desc, 32, 2)` to the common
format base and installs the W11V11U10 vtable.
