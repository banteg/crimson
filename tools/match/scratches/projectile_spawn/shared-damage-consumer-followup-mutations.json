{
  "schema": 1,
  "sites": [
    {
      "name": "shared-damage-consumer-followup",
      "find": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" float cos(float angle);\nextern \"C\" float sin(float angle);\n\nextern \"C\" int projectile_spawn(\n    const vec2f_t *pos,\n    float angle,\n    int type_id,\n    int owner_id)\n{\n    float default_damage = 1.0f;\n    int index;\n    int result;\n    projectile_t *projectile;\n    projectile_t *spawned;\n\n    if (!bonus_spawn_guard) {\n        while (\n            (owner_id == -100 || owner_id == -1 || owner_id == -2 || owner_id == -3)\n            && (\n                ++highscore_record_shots_fired,\n                type_id != PROJECTILE_TYPE_FIRE_BULLETS\n                    && (player_state_table[0].fire_bullets_timer > 0.0f\n                        || player_state_table[1].fire_bullets_timer > 0.0f)\n            )\n        ) {\n            type_id = PROJECTILE_TYPE_FIRE_BULLETS;\n        }\n    }\n\n    index = 0;\n    projectile = projectile_pool;\n    while ((int)projectile < (int)&projectile_pool[0x60]) {\n        if (!projectile->active) {\n            result = index;\n            goto initialize;\n        }\n        ++projectile;\n        ++index;\n    }\n    result = 0x5f;\n\ninitialize:\n    spawned = &projectile_pool[result];\n    spawned->fields.owner_id = owner_id;\n    spawned->active = 1;\n    spawned->fields.travel_budget = weapon_table[type_id].travel_budget;\n    spawned->position = *pos;\n    spawned->fields.origin = *pos;\n    spawned->angle = angle;\n    spawned->fields.type_id = (projectile_type_id_t)type_id;\n    spawned->fields.life_timer = 0.4f;\n    spawned->fields.reserved = 0.0f;\n    spawned->fields.speed_scale = 1.0f;\n    spawned->fields.velocity.x = (float)(cos(angle) * 1.5f);\n    spawned->fields.velocity.y = (float)(sin(angle) * 1.5f);\n\n    if (type_id == PROJECTILE_TYPE_ION_MINIGUN) {\n        spawned->fields.hit_radius = 3.0f;\n        spawned->fields.damage_pool = default_damage;\n        return result;\n    }\n    if (type_id == PROJECTILE_TYPE_ION_RIFLE) {\n        spawned->fields.hit_radius = 5.0f;\n        spawned->fields.damage_pool = default_damage;\n        return result;\n    }\n    if (type_id == PROJECTILE_TYPE_ION_CANNON || type_id == PROJECTILE_TYPE_PLASMA_CANNON) {\n        spawned->fields.hit_radius = 10.0f;\n    } else {\n        spawned->fields.hit_radius = 1.0f;\n        if (type_id == PROJECTILE_TYPE_GAUSS_GUN) {\n            spawned->fields.damage_pool = 300.0f;\n            return result;\n        }\n        if (type_id == PROJECTILE_TYPE_FIRE_BULLETS) {\n            spawned->fields.damage_pool = 240.0f;\n            return result;\n        }\n        if (type_id == PROJECTILE_TYPE_BLADE_GUN) {\n            spawned->fields.damage_pool = 50.0f;\n            return result;\n        }\n    }\n    spawned->fields.damage_pool = default_damage;\n    return result;\n}\n",
      "replacements": [
        {
          "name": "chain_baseline",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" float cos(float angle);\nextern \"C\" float sin(float angle);\n\nextern \"C\" int projectile_spawn(\n    const vec2f_t *pos,\n    float angle,\n    int type_id,\n    int owner_id)\n{\n    float default_damage = 1.0f;\n    int index;\n    int result;\n    projectile_t *projectile;\n    projectile_t *spawned;\n\n    if (!bonus_spawn_guard) {\n        while (\n            (owner_id == -100 || owner_id == -1 || owner_id == -2 || owner_id == -3)\n            && (\n                ++highscore_record_shots_fired,\n                type_id != PROJECTILE_TYPE_FIRE_BULLETS\n                    && (player_state_table[0].fire_bullets_timer > 0.0f\n                        || player_state_table[1].fire_bullets_timer > 0.0f)\n            )\n        ) {\n            type_id = PROJECTILE_TYPE_FIRE_BULLETS;\n        }\n    }\n\n    index = 0;\n    projectile = projectile_pool;\n    while ((int)projectile < (int)&projectile_pool[0x60]) {\n        if (!projectile->active) {\n            result = index;\n            goto initialize;\n        }\n        ++projectile;\n        ++index;\n    }\n    result = 0x5f;\n\ninitialize:\n    spawned = &projectile_pool[result];\n    spawned->fields.owner_id = owner_id;\n    spawned->active = 1;\n    spawned->fields.travel_budget = weapon_table[type_id].travel_budget;\n    spawned->position = *pos;\n    spawned->fields.origin = *pos;\n    spawned->angle = angle;\n    spawned->fields.type_id = (projectile_type_id_t)type_id;\n    spawned->fields.life_timer = 0.4f;\n    spawned->fields.reserved = 0.0f;\n    spawned->fields.speed_scale = 1.0f;\n    spawned->fields.velocity.x = (float)(cos(angle) * 1.5f);\n    spawned->fields.velocity.y = (float)(sin(angle) * 1.5f);\n\n    if (type_id == PROJECTILE_TYPE_ION_MINIGUN) {\n        spawned->fields.hit_radius = 3.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_RIFLE) {\n        spawned->fields.hit_radius = 5.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_CANNON || type_id == PROJECTILE_TYPE_PLASMA_CANNON) {\n        spawned->fields.hit_radius = 10.0f;\n    } else {\n        spawned->fields.hit_radius = 1.0f;\n        if (type_id == PROJECTILE_TYPE_GAUSS_GUN) {\n            default_damage = 300.0f;\n        } else if (type_id == PROJECTILE_TYPE_FIRE_BULLETS) {\n            default_damage = 240.0f;\n        } else if (type_id == PROJECTILE_TYPE_BLADE_GUN) {\n            default_damage = 50.0f;\n        }\n    }\n    spawned->fields.damage_pool = default_damage;\n    return result;\n}\n"
        },
        {
          "name": "chain_reset",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" float cos(float angle);\nextern \"C\" float sin(float angle);\n\nextern \"C\" int projectile_spawn(\n    const vec2f_t *pos,\n    float angle,\n    int type_id,\n    int owner_id)\n{\n    float default_damage = 1.0f;\n    int index;\n    int result;\n    projectile_t *projectile;\n    projectile_t *spawned;\n\n    if (!bonus_spawn_guard) {\n        while (\n            (owner_id == -100 || owner_id == -1 || owner_id == -2 || owner_id == -3)\n            && (\n                ++highscore_record_shots_fired,\n                type_id != PROJECTILE_TYPE_FIRE_BULLETS\n                    && (player_state_table[0].fire_bullets_timer > 0.0f\n                        || player_state_table[1].fire_bullets_timer > 0.0f)\n            )\n        ) {\n            type_id = PROJECTILE_TYPE_FIRE_BULLETS;\n            default_damage = 1.0f;\n        }\n    }\n\n    index = 0;\n    projectile = projectile_pool;\n    while ((int)projectile < (int)&projectile_pool[0x60]) {\n        if (!projectile->active) {\n            result = index;\n            goto initialize;\n        }\n        ++projectile;\n        ++index;\n    }\n    result = 0x5f;\n\ninitialize:\n    spawned = &projectile_pool[result];\n    spawned->fields.owner_id = owner_id;\n    spawned->active = 1;\n    spawned->fields.travel_budget = weapon_table[type_id].travel_budget;\n    spawned->position = *pos;\n    spawned->fields.origin = *pos;\n    spawned->angle = angle;\n    spawned->fields.type_id = (projectile_type_id_t)type_id;\n    spawned->fields.life_timer = 0.4f;\n    spawned->fields.reserved = 0.0f;\n    spawned->fields.speed_scale = 1.0f;\n    spawned->fields.velocity.x = (float)(cos(angle) * 1.5f);\n    spawned->fields.velocity.y = (float)(sin(angle) * 1.5f);\n\n    if (type_id == PROJECTILE_TYPE_ION_MINIGUN) {\n        spawned->fields.hit_radius = 3.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_RIFLE) {\n        spawned->fields.hit_radius = 5.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_CANNON || type_id == PROJECTILE_TYPE_PLASMA_CANNON) {\n        spawned->fields.hit_radius = 10.0f;\n    } else {\n        spawned->fields.hit_radius = 1.0f;\n        if (type_id == PROJECTILE_TYPE_GAUSS_GUN) {\n            default_damage = 300.0f;\n        } else if (type_id == PROJECTILE_TYPE_FIRE_BULLETS) {\n            default_damage = 240.0f;\n        } else if (type_id == PROJECTILE_TYPE_BLADE_GUN) {\n            default_damage = 50.0f;\n        }\n    }\n    spawned->fields.damage_pool = default_damage;\n    return result;\n}\n"
        },
        {
          "name": "chain_recursive",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" float cos(float angle);\nextern \"C\" float sin(float angle);\n\nextern \"C\" int projectile_spawn(\n    const vec2f_t *pos,\n    float angle,\n    int type_id,\n    int owner_id)\n{\n    float default_damage = 1.0f;\n    int index;\n    int result;\n    projectile_t *projectile;\n    projectile_t *spawned;\n\n    if (!bonus_spawn_guard) {\n        if (owner_id == -100 || owner_id == -1 || owner_id == -2 || owner_id == -3) {\n            ++highscore_record_shots_fired;\n            if (type_id != PROJECTILE_TYPE_FIRE_BULLETS\n                && (player_state_table[0].fire_bullets_timer > 0.0f\n                    || player_state_table[1].fire_bullets_timer > 0.0f)) {\n                return projectile_spawn(\n                    pos, angle, PROJECTILE_TYPE_FIRE_BULLETS, owner_id);\n            }\n        }\n    }\n\n    index = 0;\n    projectile = projectile_pool;\n    while ((int)projectile < (int)&projectile_pool[0x60]) {\n        if (!projectile->active) {\n            result = index;\n            goto initialize;\n        }\n        ++projectile;\n        ++index;\n    }\n    result = 0x5f;\n\ninitialize:\n    spawned = &projectile_pool[result];\n    spawned->fields.owner_id = owner_id;\n    spawned->active = 1;\n    spawned->fields.travel_budget = weapon_table[type_id].travel_budget;\n    spawned->position = *pos;\n    spawned->fields.origin = *pos;\n    spawned->angle = angle;\n    spawned->fields.type_id = (projectile_type_id_t)type_id;\n    spawned->fields.life_timer = 0.4f;\n    spawned->fields.reserved = 0.0f;\n    spawned->fields.speed_scale = 1.0f;\n    spawned->fields.velocity.x = (float)(cos(angle) * 1.5f);\n    spawned->fields.velocity.y = (float)(sin(angle) * 1.5f);\n\n    if (type_id == PROJECTILE_TYPE_ION_MINIGUN) {\n        spawned->fields.hit_radius = 3.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_RIFLE) {\n        spawned->fields.hit_radius = 5.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_CANNON || type_id == PROJECTILE_TYPE_PLASMA_CANNON) {\n        spawned->fields.hit_radius = 10.0f;\n    } else {\n        spawned->fields.hit_radius = 1.0f;\n        if (type_id == PROJECTILE_TYPE_GAUSS_GUN) {\n            default_damage = 300.0f;\n        } else if (type_id == PROJECTILE_TYPE_FIRE_BULLETS) {\n            default_damage = 240.0f;\n        } else if (type_id == PROJECTILE_TYPE_BLADE_GUN) {\n            default_damage = 50.0f;\n        }\n    }\n    spawned->fields.damage_pool = default_damage;\n    return result;\n}\n"
        },
        {
          "name": "separate-ifs_baseline",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" float cos(float angle);\nextern \"C\" float sin(float angle);\n\nextern \"C\" int projectile_spawn(\n    const vec2f_t *pos,\n    float angle,\n    int type_id,\n    int owner_id)\n{\n    float default_damage = 1.0f;\n    int index;\n    int result;\n    projectile_t *projectile;\n    projectile_t *spawned;\n\n    if (!bonus_spawn_guard) {\n        while (\n            (owner_id == -100 || owner_id == -1 || owner_id == -2 || owner_id == -3)\n            && (\n                ++highscore_record_shots_fired,\n                type_id != PROJECTILE_TYPE_FIRE_BULLETS\n                    && (player_state_table[0].fire_bullets_timer > 0.0f\n                        || player_state_table[1].fire_bullets_timer > 0.0f)\n            )\n        ) {\n            type_id = PROJECTILE_TYPE_FIRE_BULLETS;\n        }\n    }\n\n    index = 0;\n    projectile = projectile_pool;\n    while ((int)projectile < (int)&projectile_pool[0x60]) {\n        if (!projectile->active) {\n            result = index;\n            goto initialize;\n        }\n        ++projectile;\n        ++index;\n    }\n    result = 0x5f;\n\ninitialize:\n    spawned = &projectile_pool[result];\n    spawned->fields.owner_id = owner_id;\n    spawned->active = 1;\n    spawned->fields.travel_budget = weapon_table[type_id].travel_budget;\n    spawned->position = *pos;\n    spawned->fields.origin = *pos;\n    spawned->angle = angle;\n    spawned->fields.type_id = (projectile_type_id_t)type_id;\n    spawned->fields.life_timer = 0.4f;\n    spawned->fields.reserved = 0.0f;\n    spawned->fields.speed_scale = 1.0f;\n    spawned->fields.velocity.x = (float)(cos(angle) * 1.5f);\n    spawned->fields.velocity.y = (float)(sin(angle) * 1.5f);\n\n    if (type_id == PROJECTILE_TYPE_ION_MINIGUN) {\n        spawned->fields.hit_radius = 3.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_RIFLE) {\n        spawned->fields.hit_radius = 5.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_CANNON || type_id == PROJECTILE_TYPE_PLASMA_CANNON) {\n        spawned->fields.hit_radius = 10.0f;\n    } else {\n        spawned->fields.hit_radius = 1.0f;\n        if (type_id == PROJECTILE_TYPE_GAUSS_GUN) {\n            default_damage = 300.0f;\n        }\n        if (type_id == PROJECTILE_TYPE_FIRE_BULLETS) {\n            default_damage = 240.0f;\n        }\n        if (type_id == PROJECTILE_TYPE_BLADE_GUN) {\n            default_damage = 50.0f;\n        }\n    }\n    spawned->fields.damage_pool = default_damage;\n    return result;\n}\n"
        },
        {
          "name": "separate-ifs_reset",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" float cos(float angle);\nextern \"C\" float sin(float angle);\n\nextern \"C\" int projectile_spawn(\n    const vec2f_t *pos,\n    float angle,\n    int type_id,\n    int owner_id)\n{\n    float default_damage = 1.0f;\n    int index;\n    int result;\n    projectile_t *projectile;\n    projectile_t *spawned;\n\n    if (!bonus_spawn_guard) {\n        while (\n            (owner_id == -100 || owner_id == -1 || owner_id == -2 || owner_id == -3)\n            && (\n                ++highscore_record_shots_fired,\n                type_id != PROJECTILE_TYPE_FIRE_BULLETS\n                    && (player_state_table[0].fire_bullets_timer > 0.0f\n                        || player_state_table[1].fire_bullets_timer > 0.0f)\n            )\n        ) {\n            type_id = PROJECTILE_TYPE_FIRE_BULLETS;\n            default_damage = 1.0f;\n        }\n    }\n\n    index = 0;\n    projectile = projectile_pool;\n    while ((int)projectile < (int)&projectile_pool[0x60]) {\n        if (!projectile->active) {\n            result = index;\n            goto initialize;\n        }\n        ++projectile;\n        ++index;\n    }\n    result = 0x5f;\n\ninitialize:\n    spawned = &projectile_pool[result];\n    spawned->fields.owner_id = owner_id;\n    spawned->active = 1;\n    spawned->fields.travel_budget = weapon_table[type_id].travel_budget;\n    spawned->position = *pos;\n    spawned->fields.origin = *pos;\n    spawned->angle = angle;\n    spawned->fields.type_id = (projectile_type_id_t)type_id;\n    spawned->fields.life_timer = 0.4f;\n    spawned->fields.reserved = 0.0f;\n    spawned->fields.speed_scale = 1.0f;\n    spawned->fields.velocity.x = (float)(cos(angle) * 1.5f);\n    spawned->fields.velocity.y = (float)(sin(angle) * 1.5f);\n\n    if (type_id == PROJECTILE_TYPE_ION_MINIGUN) {\n        spawned->fields.hit_radius = 3.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_RIFLE) {\n        spawned->fields.hit_radius = 5.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_CANNON || type_id == PROJECTILE_TYPE_PLASMA_CANNON) {\n        spawned->fields.hit_radius = 10.0f;\n    } else {\n        spawned->fields.hit_radius = 1.0f;\n        if (type_id == PROJECTILE_TYPE_GAUSS_GUN) {\n            default_damage = 300.0f;\n        }\n        if (type_id == PROJECTILE_TYPE_FIRE_BULLETS) {\n            default_damage = 240.0f;\n        }\n        if (type_id == PROJECTILE_TYPE_BLADE_GUN) {\n            default_damage = 50.0f;\n        }\n    }\n    spawned->fields.damage_pool = default_damage;\n    return result;\n}\n"
        },
        {
          "name": "separate-ifs_recursive",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" float cos(float angle);\nextern \"C\" float sin(float angle);\n\nextern \"C\" int projectile_spawn(\n    const vec2f_t *pos,\n    float angle,\n    int type_id,\n    int owner_id)\n{\n    float default_damage = 1.0f;\n    int index;\n    int result;\n    projectile_t *projectile;\n    projectile_t *spawned;\n\n    if (!bonus_spawn_guard) {\n        if (owner_id == -100 || owner_id == -1 || owner_id == -2 || owner_id == -3) {\n            ++highscore_record_shots_fired;\n            if (type_id != PROJECTILE_TYPE_FIRE_BULLETS\n                && (player_state_table[0].fire_bullets_timer > 0.0f\n                    || player_state_table[1].fire_bullets_timer > 0.0f)) {\n                return projectile_spawn(\n                    pos, angle, PROJECTILE_TYPE_FIRE_BULLETS, owner_id);\n            }\n        }\n    }\n\n    index = 0;\n    projectile = projectile_pool;\n    while ((int)projectile < (int)&projectile_pool[0x60]) {\n        if (!projectile->active) {\n            result = index;\n            goto initialize;\n        }\n        ++projectile;\n        ++index;\n    }\n    result = 0x5f;\n\ninitialize:\n    spawned = &projectile_pool[result];\n    spawned->fields.owner_id = owner_id;\n    spawned->active = 1;\n    spawned->fields.travel_budget = weapon_table[type_id].travel_budget;\n    spawned->position = *pos;\n    spawned->fields.origin = *pos;\n    spawned->angle = angle;\n    spawned->fields.type_id = (projectile_type_id_t)type_id;\n    spawned->fields.life_timer = 0.4f;\n    spawned->fields.reserved = 0.0f;\n    spawned->fields.speed_scale = 1.0f;\n    spawned->fields.velocity.x = (float)(cos(angle) * 1.5f);\n    spawned->fields.velocity.y = (float)(sin(angle) * 1.5f);\n\n    if (type_id == PROJECTILE_TYPE_ION_MINIGUN) {\n        spawned->fields.hit_radius = 3.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_RIFLE) {\n        spawned->fields.hit_radius = 5.0f;\n    } else if (type_id == PROJECTILE_TYPE_ION_CANNON || type_id == PROJECTILE_TYPE_PLASMA_CANNON) {\n        spawned->fields.hit_radius = 10.0f;\n    } else {\n        spawned->fields.hit_radius = 1.0f;\n        if (type_id == PROJECTILE_TYPE_GAUSS_GUN) {\n            default_damage = 300.0f;\n        }\n        if (type_id == PROJECTILE_TYPE_FIRE_BULLETS) {\n            default_damage = 240.0f;\n        }\n        if (type_id == PROJECTILE_TYPE_BLADE_GUN) {\n            default_damage = 50.0f;\n        }\n    }\n    spawned->fields.damage_pool = default_damage;\n    return result;\n}\n"
        },
        {
          "name": "switch_baseline",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" float cos(float angle);\nextern \"C\" float sin(float angle);\n\nextern \"C\" int projectile_spawn(\n    const vec2f_t *pos,\n    float angle,\n    int type_id,\n    int owner_id)\n{\n    float default_damage = 1.0f;\n    int index;\n    int result;\n    projectile_t *projectile;\n    projectile_t *spawned;\n\n    if (!bonus_spawn_guard) {\n        while (\n            (owner_id == -100 || owner_id == -1 || owner_id == -2 || owner_id == -3)\n            && (\n                ++highscore_record_shots_fired,\n                type_id != PROJECTILE_TYPE_FIRE_BULLETS\n                    && (player_state_table[0].fire_bullets_timer > 0.0f\n                        || player_state_table[1].fire_bullets_timer > 0.0f)\n            )\n        ) {\n            type_id = PROJECTILE_TYPE_FIRE_BULLETS;\n        }\n    }\n\n    index = 0;\n    projectile = projectile_pool;\n    while ((int)projectile < (int)&projectile_pool[0x60]) {\n        if (!projectile->active) {\n            result = index;\n            goto initialize;\n        }\n        ++projectile;\n        ++index;\n    }\n    result = 0x5f;\n\ninitialize:\n    spawned = &projectile_pool[result];\n    spawned->fields.owner_id = owner_id;\n    spawned->active = 1;\n    spawned->fields.travel_budget = weapon_table[type_id].travel_budget;\n    spawned->position = *pos;\n    spawned->fields.origin = *pos;\n    spawned->angle = angle;\n    spawned->fields.type_id = (projectile_type_id_t)type_id;\n    spawned->fields.life_timer = 0.4f;\n    spawned->fields.reserved = 0.0f;\n    spawned->fields.speed_scale = 1.0f;\n    spawned->fields.velocity.x = (float)(cos(angle) * 1.5f);\n    spawned->fields.velocity.y = (float)(sin(angle) * 1.5f);\n\n    switch (type_id) {\n    case PROJECTILE_TYPE_ION_MINIGUN:\n        spawned->fields.hit_radius = 3.0f;\n        break;\n    case PROJECTILE_TYPE_ION_RIFLE:\n        spawned->fields.hit_radius = 5.0f;\n        break;\n    case PROJECTILE_TYPE_ION_CANNON:\n    case PROJECTILE_TYPE_PLASMA_CANNON:\n        spawned->fields.hit_radius = 10.0f;\n        break;\n    default:\n        spawned->fields.hit_radius = 1.0f;\n        if (type_id == PROJECTILE_TYPE_GAUSS_GUN) default_damage = 300.0f;\n        else if (type_id == PROJECTILE_TYPE_FIRE_BULLETS) default_damage = 240.0f;\n        else if (type_id == PROJECTILE_TYPE_BLADE_GUN) default_damage = 50.0f;\n    }\n    spawned->fields.damage_pool = default_damage;\n    return result;\n}\n"
        },
        {
          "name": "switch_reset",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" float cos(float angle);\nextern \"C\" float sin(float angle);\n\nextern \"C\" int projectile_spawn(\n    const vec2f_t *pos,\n    float angle,\n    int type_id,\n    int owner_id)\n{\n    float default_damage = 1.0f;\n    int index;\n    int result;\n    projectile_t *projectile;\n    projectile_t *spawned;\n\n    if (!bonus_spawn_guard) {\n        while (\n            (owner_id == -100 || owner_id == -1 || owner_id == -2 || owner_id == -3)\n            && (\n                ++highscore_record_shots_fired,\n                type_id != PROJECTILE_TYPE_FIRE_BULLETS\n                    && (player_state_table[0].fire_bullets_timer > 0.0f\n                        || player_state_table[1].fire_bullets_timer > 0.0f)\n            )\n        ) {\n            type_id = PROJECTILE_TYPE_FIRE_BULLETS;\n            default_damage = 1.0f;\n        }\n    }\n\n    index = 0;\n    projectile = projectile_pool;\n    while ((int)projectile < (int)&projectile_pool[0x60]) {\n        if (!projectile->active) {\n            result = index;\n            goto initialize;\n        }\n        ++projectile;\n        ++index;\n    }\n    result = 0x5f;\n\ninitialize:\n    spawned = &projectile_pool[result];\n    spawned->fields.owner_id = owner_id;\n    spawned->active = 1;\n    spawned->fields.travel_budget = weapon_table[type_id].travel_budget;\n    spawned->position = *pos;\n    spawned->fields.origin = *pos;\n    spawned->angle = angle;\n    spawned->fields.type_id = (projectile_type_id_t)type_id;\n    spawned->fields.life_timer = 0.4f;\n    spawned->fields.reserved = 0.0f;\n    spawned->fields.speed_scale = 1.0f;\n    spawned->fields.velocity.x = (float)(cos(angle) * 1.5f);\n    spawned->fields.velocity.y = (float)(sin(angle) * 1.5f);\n\n    switch (type_id) {\n    case PROJECTILE_TYPE_ION_MINIGUN:\n        spawned->fields.hit_radius = 3.0f;\n        break;\n    case PROJECTILE_TYPE_ION_RIFLE:\n        spawned->fields.hit_radius = 5.0f;\n        break;\n    case PROJECTILE_TYPE_ION_CANNON:\n    case PROJECTILE_TYPE_PLASMA_CANNON:\n        spawned->fields.hit_radius = 10.0f;\n        break;\n    default:\n        spawned->fields.hit_radius = 1.0f;\n        if (type_id == PROJECTILE_TYPE_GAUSS_GUN) default_damage = 300.0f;\n        else if (type_id == PROJECTILE_TYPE_FIRE_BULLETS) default_damage = 240.0f;\n        else if (type_id == PROJECTILE_TYPE_BLADE_GUN) default_damage = 50.0f;\n    }\n    spawned->fields.damage_pool = default_damage;\n    return result;\n}\n"
        },
        {
          "name": "switch_recursive",
          "text": "#include \"crimsonland_gameplay.h\"\n\nextern \"C\" float cos(float angle);\nextern \"C\" float sin(float angle);\n\nextern \"C\" int projectile_spawn(\n    const vec2f_t *pos,\n    float angle,\n    int type_id,\n    int owner_id)\n{\n    float default_damage = 1.0f;\n    int index;\n    int result;\n    projectile_t *projectile;\n    projectile_t *spawned;\n\n    if (!bonus_spawn_guard) {\n        if (owner_id == -100 || owner_id == -1 || owner_id == -2 || owner_id == -3) {\n            ++highscore_record_shots_fired;\n            if (type_id != PROJECTILE_TYPE_FIRE_BULLETS\n                && (player_state_table[0].fire_bullets_timer > 0.0f\n                    || player_state_table[1].fire_bullets_timer > 0.0f)) {\n                return projectile_spawn(\n                    pos, angle, PROJECTILE_TYPE_FIRE_BULLETS, owner_id);\n            }\n        }\n    }\n\n    index = 0;\n    projectile = projectile_pool;\n    while ((int)projectile < (int)&projectile_pool[0x60]) {\n        if (!projectile->active) {\n            result = index;\n            goto initialize;\n        }\n        ++projectile;\n        ++index;\n    }\n    result = 0x5f;\n\ninitialize:\n    spawned = &projectile_pool[result];\n    spawned->fields.owner_id = owner_id;\n    spawned->active = 1;\n    spawned->fields.travel_budget = weapon_table[type_id].travel_budget;\n    spawned->position = *pos;\n    spawned->fields.origin = *pos;\n    spawned->angle = angle;\n    spawned->fields.type_id = (projectile_type_id_t)type_id;\n    spawned->fields.life_timer = 0.4f;\n    spawned->fields.reserved = 0.0f;\n    spawned->fields.speed_scale = 1.0f;\n    spawned->fields.velocity.x = (float)(cos(angle) * 1.5f);\n    spawned->fields.velocity.y = (float)(sin(angle) * 1.5f);\n\n    switch (type_id) {\n    case PROJECTILE_TYPE_ION_MINIGUN:\n        spawned->fields.hit_radius = 3.0f;\n        break;\n    case PROJECTILE_TYPE_ION_RIFLE:\n        spawned->fields.hit_radius = 5.0f;\n        break;\n    case PROJECTILE_TYPE_ION_CANNON:\n    case PROJECTILE_TYPE_PLASMA_CANNON:\n        spawned->fields.hit_radius = 10.0f;\n        break;\n    default:\n        spawned->fields.hit_radius = 1.0f;\n        if (type_id == PROJECTILE_TYPE_GAUSS_GUN) default_damage = 300.0f;\n        else if (type_id == PROJECTILE_TYPE_FIRE_BULLETS) default_damage = 240.0f;\n        else if (type_id == PROJECTILE_TYPE_BLADE_GUN) default_damage = 50.0f;\n    }\n    spawned->fields.damage_pool = default_damage;\n    return result;\n}\n"
        }
      ]
    }
  ]
}
