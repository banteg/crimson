# `grim_pixel_format_ctor_dxt2`

Native target: `grim.dll` at `0x1001b412` (24 bytes).

This derived constructor delegates the descriptor to the shared DXT-format
initializer and installs the DXT2 vtable.
