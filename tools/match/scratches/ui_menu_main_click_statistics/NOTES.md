# ui_menu_main_click_statistics

Native target: `crimsonland.exe` at `0x00447390` (67 bytes).

The callback queues the statistics menu, mutes all three menu-music slots, and
then starts the shortie-monk track exclusively. Natural C++ matches all 16
instructions and references `10/0/0`.
