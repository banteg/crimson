# `grim_pixel_format_ctor_q8w8v8u8`

Native target: `grim.dll` at `0x1001b055` (28 bytes).

This derived pixel-format constructor delegates `(desc, 32, 3)` to the common
format base and installs the Q8W8V8U8 vtable.
