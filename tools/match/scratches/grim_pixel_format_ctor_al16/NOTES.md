# `grim_pixel_format_ctor_al16`

Native target: `grim.dll` at `0x1001b3a6` (28 bytes).

This derived pixel-format constructor delegates `(desc, 32, 1)` to the common
format base and installs the AL16 vtable.
