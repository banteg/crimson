# `grim_pixel_format_ctor_yuy2`

Native target: `grim.dll` at `0x1001b4f4` (24 bytes).

This derived constructor delegates the descriptor to the shared YUV-format
initializer and installs the YUY2 vtable.
