# ui_menu_main_click_mods

Native target: `crimsonland.exe` at `0x00447350` (18 bytes).

The callback clears the transition direction and queues the mods-menu state.
Natural C++ matches all 3 instructions and references `2/0/0`.
