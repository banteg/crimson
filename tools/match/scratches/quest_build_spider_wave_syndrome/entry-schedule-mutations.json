{
  "schema": 1,
  "sites": [
    {
      "name": "entry-schedule",
      "find": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        ++builder.count;\n        spawn->set_spawn(\n            SPAWN_ID_SPIDER_SMALL_BLUE_40,\n            trigger_time_ms);\n        spawn->count = config_blob.player_count * 2 + 6;\n",
      "replacements": [
        {
          "name": "position-setter",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.set(\n            -64.0f,\n            (float)(terrain_texture_width / 2));\n        ++builder.count;\n        spawn->set_spawn(\n            SPAWN_ID_SPIDER_SMALL_BLUE_40,\n            trigger_time_ms);\n        spawn->count = config_blob.player_count * 2 + 6;\n"
        },
        {
          "name": "increment-before-y",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        ++builder.count;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        spawn->set_spawn(\n            SPAWN_ID_SPIDER_SMALL_BLUE_40,\n            trigger_time_ms);\n        spawn->count = config_blob.player_count * 2 + 6;\n"
        },
        {
          "name": "named-half-increment-before-y",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        int half_width = terrain_texture_width / 2;\n        ++builder.count;\n        spawn->pos.y = (float)half_width;\n        spawn->set_spawn(\n            SPAWN_ID_SPIDER_SMALL_BLUE_40,\n            trigger_time_ms);\n        spawn->count = config_blob.player_count * 2 + 6;\n"
        },
        {
          "name": "postincremented-entry-index",
          "text": "        quest_entry_original_t *spawn =\n            &builder.spawns[builder.count++];\n        spawn->pos.x = -64.0f;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        spawn->set_spawn(\n            SPAWN_ID_SPIDER_SMALL_BLUE_40,\n            trigger_time_ms);\n        spawn->count = config_blob.player_count * 2 + 6;\n"
        },
        {
          "name": "direct-metadata-stores",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        ++builder.count;\n        spawn->template_id = SPAWN_ID_SPIDER_SMALL_BLUE_40;\n        spawn->trigger_time_ms = trigger_time_ms;\n        spawn->count = config_blob.player_count * 2 + 6;\n"
        },
        {
          "name": "increment-before-y-direct-metadata",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        ++builder.count;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        spawn->template_id = SPAWN_ID_SPIDER_SMALL_BLUE_40;\n        spawn->trigger_time_ms = trigger_time_ms;\n        spawn->count = config_blob.player_count * 2 + 6;\n"
        },
        {
          "name": "named-player-count",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        ++builder.count;\n        spawn->set_spawn(\n            SPAWN_ID_SPIDER_SMALL_BLUE_40,\n            trigger_time_ms);\n        int spawn_count = config_blob.player_count * 2 + 6;\n        spawn->count = spawn_count;\n"
        },
        {
          "name": "increment-before-y-named-player-count",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        ++builder.count;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        spawn->set_spawn(\n            SPAWN_ID_SPIDER_SMALL_BLUE_40,\n            trigger_time_ms);\n        int spawn_count = config_blob.player_count * 2 + 6;\n        spawn->count = spawn_count;\n"
        },
        {
          "name": "increment-before-position",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        ++builder.count;\n        spawn->pos.x = -64.0f;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        spawn->set_spawn(\n            SPAWN_ID_SPIDER_SMALL_BLUE_40,\n            trigger_time_ms);\n        spawn->count = config_blob.player_count * 2 + 6;\n"
        },
        {
          "name": "metadata-before-count-increment",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        spawn->set_spawn(\n            SPAWN_ID_SPIDER_SMALL_BLUE_40,\n            trigger_time_ms);\n        ++builder.count;\n        spawn->count = config_blob.player_count * 2 + 6;\n"
        },
        {
          "name": "metadata-before-y",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        spawn->set_spawn(\n            SPAWN_ID_SPIDER_SMALL_BLUE_40,\n            trigger_time_ms);\n        spawn->pos.y = (float)(terrain_texture_width / 2);\n        ++builder.count;\n        spawn->count = config_blob.player_count * 2 + 6;\n"
        },
        {
          "name": "native-staged-scalars",
          "text": "        quest_entry_original_t *spawn = &builder.spawns[builder.count];\n        spawn->pos.x = -64.0f;\n        int half_width = terrain_texture_width / 2;\n        ++builder.count;\n        spawn->pos.y = (float)half_width;\n        spawn->template_id = SPAWN_ID_SPIDER_SMALL_BLUE_40;\n        spawn->trigger_time_ms = trigger_time_ms;\n        int spawn_count = config_blob.player_count * 2 + 6;\n        spawn->count = spawn_count;\n"
        }
      ]
    }
  ]
}
