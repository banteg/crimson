# `grim_jpeg_memory_skip_input_data`

Native target: `grim.dll` at `0x1003aac0`, 61 bytes.

This callback advances across buffered input, refilling until the requested
positive skip fits in the current window. It is selected directly from the
canonical recovered provider translation unit under its pinned VC6 flags.
