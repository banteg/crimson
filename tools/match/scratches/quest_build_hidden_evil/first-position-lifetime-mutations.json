{
  "schema": 1,
  "sites": [
    {
      "name": "first-position-lifetime",
      "find": "    spawns[0].pos = quest_vec2_t(\n        (float)(terrain_texture_width / 2),\n        (float)(terrain_texture_height + 64));\n    spawns[0].set_spawn(\n        SPAWN_ID_ALIEN_HIDDEN_1_21,\n        500,\n        50);\n",
      "replacements": [
        {
          "name": "named-vector",
          "text": "    quest_vec2_t first_position(\n        (float)(terrain_texture_width / 2),\n        (float)(terrain_texture_height + 64));\n    spawns[0].pos = first_position;\n    spawns[0].set_spawn(\n        SPAWN_ID_ALIEN_HIDDEN_1_21,\n        500,\n        50);\n"
        },
        {
          "name": "named-scalars",
          "text": "    float first_x = (float)(terrain_texture_width / 2);\n    float first_y = (float)(terrain_texture_height + 64);\n    spawns[0].pos = quest_vec2_t(first_x, first_y);\n    spawns[0].set_spawn(\n        SPAWN_ID_ALIEN_HIDDEN_1_21,\n        500,\n        50);\n"
        },
        {
          "name": "split-components",
          "text": "    spawns[0].pos.x = (float)(terrain_texture_width / 2);\n    spawns[0].pos.y = (float)(terrain_texture_height + 64);\n    spawns[0].set_spawn(\n        SPAWN_ID_ALIEN_HIDDEN_1_21,\n        500,\n        50);\n"
        },
        {
          "name": "named-vector-split-copy",
          "text": "    quest_vec2_t first_position(\n        (float)(terrain_texture_width / 2),\n        (float)(terrain_texture_height + 64));\n    spawns[0].pos.x = first_position.x;\n    spawns[0].pos.y = first_position.y;\n    spawns[0].set_spawn(\n        SPAWN_ID_ALIEN_HIDDEN_1_21,\n        500,\n        50);\n"
        }
      ]
    }
  ]
}
