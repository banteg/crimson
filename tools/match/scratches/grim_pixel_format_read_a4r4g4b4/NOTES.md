# `grim_pixel_format_read_a4r4g4b4`

Native target: `grim.dll` at `0x1001966c` (175 bytes).

This reader expands all four packed A4R4G4B4 nibbles through `1/15` and applies
the active color key to the completed row.
